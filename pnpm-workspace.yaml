packages:
  - packages/*

# pnpm 10 blocks dependency postinstall scripts by default; these need their
# native binaries built (vite/esbuild, next/sharp, eslint resolver).
onlyBuiltDependencies:
  - esbuild
  - sharp
  - unrs-resolver
