# Generated API clients — not formatted (mirrors eslint.config.mjs ignores).
src/client
src/auth-client

# Built Storybook output.
storybook-static

# Lockfile — machine-generated.
pnpm-lock.yaml

# AI-tooling sources vendored from the vinta-ai-workflows package. Reformatting them
# would create permanent diff noise against upstream, which vinta-sync-ai-tools and
# vinta-update-project-skills compare against when offering updates.
# ai-tools/AGENTS.md and ai-tools/agents/ are ours and stay formatted.
ai-tools/skills
ai-tools/scripts

# Per-vendor files generated by `pnpm setup:ai-tools` from ai-tools/agents/*.yaml.
# Regenerated on every run — formatting them here would be overwritten. Edit the YAML.
.claude/agents
