# Base URL of the backend REST API.
# Used both at codegen time (openapi-ts) and at runtime to point the generated
# API clients at the backend (see src/lib/configure-api-clients.ts).
# Local dev defaults to http://localhost:8000 when unset.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
