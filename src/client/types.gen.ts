// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
    baseUrl: `${string}://${string}` | (string & {});
};

/**
 * Serializer for accepting invitations via public endpoint.
 */
export type AcceptInvitation = {
    token: string;
};

/**
 * * `create` - create
 * * `update` - update
 * * `delete` - delete
 */
export type ActionEnum = 'create' | 'update' | 'delete';

/**
 * * `calendar_event` - Calendar Event
 * * `calendar` - Calendar
 * * `recurrence_rule` - Recurrence Rule
 * * `external_attendee` - External Attendee
 * * `external_attendance` - External Attendance
 * * `attendance` - Attendance
 * * `user` - User
 * * `resource_allocation` - Resource Allocation
 * * `event_recurring_exception` - Event Recurring Exception
 * * `blocked_time` - Blocked Time
 * * `blocked_time_recurring_exception` - Blocked Time Recurring Exception
 * * `available_time` - Available Time
 * * `available_time_recurring_exception` - Available Time Recurring Exception
 * * `availability_windows` - Availability Windows
 * * `unavailable_windows` - Unavailable Windows
 * * `organization` - Organization
 * * `calendar_group` - Calendar Group
 * * `system_user` - System User
 * * `membership` - Membership
 * * `invitation` - Invitation
 * * `branding` - Branding
 * * `child_org_analytics` - Child Organization Analytics
 * * `calendar_booking_code` - Calendar Booking Code
 * * `create_resource_calendar` - Create Resource Calendar
 * * `disable_resource_calendar` - Disable Resource Calendar
 * * `import_resource_calendars` - Import Resource Calendars
 * * `create_availability_window` - Create Availability Window
 * * `update_availability_window` - Update Availability Window
 * * `delete_availability_window` - Delete Availability Window
 * * `batch_update_availability_windows` - Batch Update Availability Windows
 * * `create_blocked_time` - Create Blocked Time
 * * `update_blocked_time` - Update Blocked Time
 * * `delete_blocked_time` - Delete Blocked Time
 * * `calendar_bundle` - Calendar Bundle
 * * `create_calendar` - Create Calendar
 * * `update_calendar` - Update Calendar
 * * `create_calendar_bundle` - Create Calendar Bundle
 * * `update_calendar_bundle` - Update Calendar Bundle
 * * `disable_calendar_bundle` - Disable Calendar Bundle
 * * `webhook_configuration` - Webhook Configuration
 */
export type AvailableResourcesEnum = 'calendar_event' | 'calendar' | 'recurrence_rule' | 'external_attendee' | 'external_attendance' | 'attendance' | 'user' | 'resource_allocation' | 'event_recurring_exception' | 'blocked_time' | 'blocked_time_recurring_exception' | 'available_time' | 'available_time_recurring_exception' | 'availability_windows' | 'unavailable_windows' | 'organization' | 'calendar_group' | 'system_user' | 'membership' | 'invitation' | 'branding' | 'child_org_analytics' | 'calendar_booking_code' | 'create_resource_calendar' | 'disable_resource_calendar' | 'import_resource_calendars' | 'create_availability_window' | 'update_availability_window' | 'delete_availability_window' | 'batch_update_availability_windows' | 'create_blocked_time' | 'update_blocked_time' | 'delete_blocked_time' | 'calendar_bundle' | 'create_calendar' | 'update_calendar' | 'create_calendar_bundle' | 'update_calendar_bundle' | 'disable_calendar_bundle' | 'webhook_configuration';

/**
 * Serializer for AvailableTime model with recurring support.
 */
export type AvailableTime = {
    readonly id: number;
    start_time: string;
    end_time: string;
    timezone: string;
    /**
     * Recurrence rule data for creating recurring available times
     */
    recurrence_rule?: RecurrenceRule;
    /**
     * True if this is an instance of a recurring available time
     */
    readonly is_recurring_instance: boolean;
    /**
     * True if this is a recurring available time
     */
    readonly is_recurring: boolean;
    /**
     * Get parent available time for instances.
     */
    readonly parent_available_time: {
        id: number;
    } | null;
    /**
     * For recurring instances, this identifies which occurrence this is
     */
    readonly recurrence_id: string | null;
    readonly created: string;
    readonly modified: string;
    calendar?: number | null;
};

/**
 * Transactional batch of create/update/delete operations on a calendar's available times.
 *
 * All operations target a single calendar (resolved from ``calendar`` or the user's
 * default) and run in one transaction — any failure rolls the whole batch back.
 */
export type AvailableTimeBatch = {
    operations: Array<AvailableTimeOperation>;
    /**
     * Calendar to apply the batch to. Defaults to the user's default calendar.
     */
    calendar?: number | null;
};

export type AvailableTimeBulkModification = {
    modification_start_date: string;
    /**
     * Recurrence rule data for the modification range
     */
    recurrence_rule?: RecurrenceRule;
    modified_start_time_offset?: string | null;
    modified_end_time_offset?: string | null;
    is_cancelled?: boolean;
};

/**
 * A single create/update/delete operation in an available-times batch.
 */
export type AvailableTimeOperation = {
    action: ActionEnum;
    /**
     * Target AvailableTime id (required for update/delete).
     */
    id?: number;
    start_time?: string;
    end_time?: string;
    timezone?: string;
    /**
     * RRULE string; null clears recurrence. Omit to leave unchanged on update.
     */
    rrule_string?: string | null;
};

/**
 * Serializer for creating recurring available time exceptions.
 */
export type AvailableTimeRecurringException = {
    /**
     * The date of the occurrence to modify or cancel
     */
    exception_date: string;
    /**
     * New start time for the modified occurrence (if not cancelled)
     */
    modified_start_time?: string | null;
    /**
     * New end time for the modified occurrence (if not cancelled)
     */
    modified_end_time?: string | null;
    /**
     * True if cancelling the occurrence, False if modifying
     */
    is_cancelled?: boolean;
};

export type AvailableTimeWindow = {
    id: number;
    start_time: string;
    end_time: string;
    can_book_partially: boolean;
};

/**
 * Serializer for BillingAddress virtual model.
 */
export type BillingAddress = {
    readonly id: number;
    street_name: string;
    street_number: string;
    neighborhood?: string;
    address_line_2?: string;
    city: string;
    state: string;
    country: string;
    zip_code: string;
};

/**
 * Serializer for BillingProfile virtual model.
 */
export type BillingProfile = {
    readonly id: number;
    document_type: string;
    document_number: string;
    billing_address: BillingAddress;
    readonly created: string;
    readonly modified: string;
};

/**
 * Serializer for BlockedTime model with recurring support.
 */
export type BlockedTime = {
    readonly id: number;
    start_time: string;
    end_time: string;
    timezone: string;
    reason?: string;
    /**
     * Recurrence rule data for creating recurring blocked times
     */
    recurrence_rule?: RecurrenceRule;
    readonly external_id: string;
    /**
     * True if this is an instance of a recurring blocked time
     */
    readonly is_recurring_instance: boolean;
    /**
     * True if this is a recurring blocked time
     */
    readonly is_recurring: boolean;
    /**
     * Get parent blocked time for instances.
     */
    readonly parent_blocked_time: {
        id: number;
        reason: string | null;
    } | null;
    readonly created: string;
    readonly modified: string;
    calendar?: number | null;
};

export type BlockedTimeBulkModification = {
    modification_start_date: string;
    modified_reason?: string | null;
    /**
     * Recurrence rule data for the modification range
     */
    recurrence_rule?: RecurrenceRule;
    modified_start_time_offset?: string | null;
    modified_end_time_offset?: string | null;
    is_cancelled?: boolean;
};

/**
 * Serializer for creating recurring blocked time exceptions.
 */
export type BlockedTimeRecurringException = {
    /**
     * The date of the occurrence to modify or cancel
     */
    exception_date: string;
    /**
     * New reason for the modified occurrence (if not cancelled)
     */
    modified_reason?: string | null;
    /**
     * New start time for the modified occurrence (if not cancelled)
     */
    modified_start_time?: string | null;
    /**
     * New end time for the modified occurrence (if not cancelled)
     */
    modified_end_time?: string | null;
    /**
     * True if cancelling the occurrence, False if modifying
     */
    is_cancelled?: boolean;
};

export type BookableSlotProposal = {
    start_time: string;
    end_time: string;
};

/**
 * Serializer for creating multiple blocked times.
 */
export type BulkBlockedTime = {
    blocked_times: Array<BlockedTime>;
};

/**
 * Input serializer for the bulk mark-as-read endpoint.
 *
 * Validates that the request body contains a non-empty list of notification ids.
 * Empty list or missing field → DRF 400 validation error.
 */
export type BulkMarkRead = {
    ids: Array<number>;
};

export type Calendar = {
    readonly id: number;
    name: string;
    description?: string;
    readonly email: string;
    readonly external_id: string;
    provider: ProviderEnum;
    /**
     * The type of calendar. Personal calendars are for individual use, resource calendars are for shared resources, and virtual calendars are for online meetings or events.
     *
     * * `personal` - Personal Calendar
     * * `resource` - Resource Calendar
     * * `virtual` - Virtual Calendar
     * * `bundle` - Bundle Calendar
     */
    calendar_type: CalendarTypeEnum;
    /**
     * The maximum number of attendees that can be accommodated in this calendar's events. This is only applicable for resource calendars.
     */
    capacity?: number | null;
    /**
     * If true, this calendar can manage its own available time windows. If not, it will use the available time windows of the external calendar it's attached to.
     */
    manage_available_windows?: boolean;
    /**
     * Controls how this calendar appears in queries. active: listed and available for booking (default). unlisted: hidden from listing/booking queries but still synced for conflict detection; survives re-import so user opt-out is preserved. inactive: soft-deleted, hidden from all queries and not synced. Use DELETE /calendars/{id}/ to transition to inactive instead of hard-deleting.
     *
     * * `active` - Active
     * * `unlisted` - Unlisted
     * * `inactive` - Inactive
     */
    visibility?: VisibilityEnum;
    /**
     * Whether this calendar's events are pulled from the external provider. Set to False to skip syncing calendars that aren't useful for scheduling — holidays, birthdays, organization-wide events, etc. When False, no new CalendarSync is requested for this calendar (including webhook- and import-triggered syncs); previously synced events are left untouched. Default True keeps existing calendars syncing as before.
     */
    sync_enabled?: boolean;
};

export type CalendarBundleCreate = {
    name: string;
    bundle_calendars: Array<number>;
    primary_calendar: number | null;
};

export type CalendarEvent = {
    readonly id: number;
    title: string;
    description?: string;
    start_time: string;
    end_time: string;
    timezone: string;
    readonly created: string;
    readonly modified: string;
    readonly external_id: string;
    external_attendances: Array<EventExternalAttendance>;
    attendances: Array<EventAttendance>;
    resource_allocations: Array<ResourceAllocation>;
    /**
     * Recurrence rule data for creating recurring events
     */
    recurrence_rule?: RecurrenceRule;
    parent_recurring_object: ParentEvent;
    /**
     * True if this is an instance of a recurring event
     */
    readonly is_recurring_instance: boolean;
    /**
     * True if this is a recurring event
     */
    readonly is_recurring: boolean;
    /**
     * True if this object is an exception to the recurrence rule (modified occurrence)
     */
    is_recurring_exception?: boolean;
    /**
     * For recurring instances, this identifies which occurrence this is
     */
    recurrence_id?: string | null;
};

export type CalendarEventTransfer = {
    target_calendar_id: number;
};

export type CalendarGroup = {
    readonly id: number;
    name: string;
    description?: string;
    slots: Array<CalendarGroupSlot>;
    readonly created: string;
    readonly modified: string;
};

/**
 * Input for the availability action: list of [start, end] windows.
 */
export type CalendarGroupAvailabilityQuery = {
    ranges: Array<RangeInput>;
};

/**
 * Input for booking an event through a CalendarGroup.
 *
 * On `save()` this delegates to `CalendarGroupService.create_grouped_event`
 * and returns the created `CalendarEvent`. The view is responsible for
 * serializing the result (typically with `CalendarEventSerializer`).
 */
export type CalendarGroupEventCreate = {
    title: string;
    description?: string;
    start_time: string;
    end_time: string;
    timezone: string;
    slot_selections: Array<CalendarGroupSlotSelectionInput>;
    attendances?: Array<{
        [key: string]: unknown;
    }>;
    external_attendances?: Array<{
        [key: string]: unknown;
    }>;
};

export type CalendarGroupRangeAvailability = {
    start_time: string;
    end_time: string;
    slots: Array<CalendarGroupSlotAvailability>;
};

/**
 * Nested slot representation used inside CalendarGroupSerializer.
 *
 * On write, accepts `calendar_ids: list[int]`; on read exposes the calendar
 * pool via `calendars` (the M2M). We deliberately keep slot writes to
 * payload-time data only — persistence happens through
 * `CalendarGroupSerializer` which delegates to `CalendarGroupService`.
 */
export type CalendarGroupSlot = {
    readonly id: number;
    name: string;
    description?: string;
    order?: number;
    /**
     * How many calendars from the pool must be selected when booking. Default 1; use larger values when a slot needs multiple calendars (e.g. two nurses).
     */
    required_count?: number;
    readonly calendars: Array<Calendar>;
};

export type CalendarGroupSlotAvailability = {
    slot_id: number;
    available_calendar_ids: Array<number>;
    required_count: number;
    readonly is_bookable: boolean;
};

export type CalendarSync = {
    readonly id: number;
    status: CalendarSyncStatusEnum;
    start_datetime: string;
    end_datetime: string;
    should_update_events: boolean;
    /**
     * What kicked off this sync: import, manual, webhook, or admin.
     *
     * * `import` - Import
     * * `manual` - Manual
     * * `webhook` - Webhook
     * * `admin` - Admin
     */
    trigger_source: TriggerSourceEnum;
    readonly error_message: string;
};

export type CalendarSyncRequest = {
    start_datetime: string;
    end_datetime: string;
    should_update_events?: boolean;
};

/**
 * * `success` - Success
 * * `failed` - Failed
 * * `in_progress` - In Progress
 * * `not_started` - Not Started
 */
export type CalendarSyncStatusEnum = 'success' | 'failed' | 'in_progress' | 'not_started';

/**
 * * `personal` - Personal Calendar
 * * `resource` - Resource Calendar
 * * `virtual` - Virtual Calendar
 * * `bundle` - Bundle Calendar
 */
export type CalendarTypeEnum = 'personal' | 'resource' | 'virtual' | 'bundle';

/**
 * Read-only serializer for the caller's current organization membership.
 *
 * Returns the membership role and the nested organization so the frontend
 * can distinguish between an onboarded user and a gated (membership-less) user.
 */
export type CurrentMembership = {
    /**
     * Role the user holds in this organization. Admins can manage organization-scoped resources (e.g. CalendarGroups) regardless of direct ownership.
     *
     * * `member` - Member
     * * `admin` - Admin
     */
    role: RoleEnum;
    /**
     * Serialize the related organization using OrganizationSerializer.
     */
    readonly organization: {
        [key: string]: unknown;
    };
};

export type EventAttendance = {
    /**
     * ID of the external attendee.
     */
    id?: number | null;
    membership: OwnershipMembership;
    status: RsvpStatusEnum;
    readonly created: string;
    readonly modified: string;
};

/**
 * Serializer for creating bulk modifications on recurring events from a given date.
 */
export type EventBulkModification = {
    modification_start_date: string;
    modified_title?: string | null;
    modified_description?: string | null;
    /**
     * Recurrence rule data for the modification range
     */
    recurrence_rule?: RecurrenceRule;
    modified_start_time_offset?: string | null;
    modified_end_time_offset?: string | null;
    is_cancelled?: boolean;
};

export type EventExternalAttendance = {
    /**
     * ID of the external attendee.
     */
    id?: number | null;
    external_attendee: ExternalAttendee;
    status: RsvpStatusEnum;
    readonly created: string;
    readonly modified: string;
};

/**
 * Serializer for creating recurring event exceptions.
 */
export type EventRecurringException = {
    /**
     * The date of the occurrence to modify or cancel
     */
    exception_date: string;
    /**
     * New title for the modified occurrence (if not cancelled)
     */
    modified_title?: string | null;
    /**
     * New description for the modified occurrence (if not cancelled)
     */
    modified_description?: string | null;
    /**
     * New start time for the modified occurrence (if not cancelled)
     */
    modified_start_time?: string | null;
    /**
     * New end time for the modified occurrence (if not cancelled)
     */
    modified_end_time?: string | null;
    /**
     * True if cancelling the occurrence, False if modifying
     */
    is_cancelled?: boolean;
};

/**
 * * `calendar_event_created` - Calendar Event Created
 * * `calendar_event_updated` - Calendar Event Updated
 * * `calendar_event_deleted` - Calendar Event Deleted
 * * `calendar_event_attendee_added` - Calendar Event Attendee Added
 * * `calendar_event_attendee_removed` - Calendar Event Attendee Removed
 * * `calendar_event_attendee_updated` - Calendar Event Attendee Updated
 * * `organization_member_created` - Organization member created
 */
export type EventTypeEnum = 'calendar_event_created' | 'calendar_event_updated' | 'calendar_event_deleted' | 'calendar_event_attendee_added' | 'calendar_event_attendee_removed' | 'calendar_event_attendee_updated' | 'organization_member_created';

export type ExternalAttendee = {
    /**
     * ID of the external attendee.
     */
    id?: number | null;
    name?: string;
    email: string;
    readonly created: string;
    readonly modified: string;
};

/**
 * * `DAILY` - Daily
 * * `WEEKLY` - Weekly
 * * `MONTHLY` - Monthly
 * * `YEARLY` - Yearly
 */
export type FrequencyEnum = 'DAILY' | 'WEEKLY' | 'MONTHLY' | 'YEARLY';

/**
 * Read-only serializer for the caller's active organization memberships.
 *
 * Used by ``GET /organizations/mine/`` to power the frontend org switcher.
 * Returns a list of ``{organization: {id, name}, role}`` entries — one per
 * active membership — without requiring the ``X-Organization-Id`` header.
 */
export type MyMembership = {
    organization: OrganizationBrief;
    /**
     * Role the user holds in this organization. Admins can manage organization-scoped resources (e.g. CalendarGroups) regardless of direct ownership.
     *
     * * `member` - Member
     * * `admin` - Admin
     */
    role: RoleEnum;
};

/**
 * Read-only serializer for in-app notification objects.
 *
 * Works for both vintasend Notification dataclasses (Phase 1 — returned by
 * get_in_app_unread) and vintasend_django model instances (Phase 2 — ORM rows).
 *
 * Fields:
 * - id, title, notification_type, status: present on both dataclass and model.
 * - body: rendered at read time via body_template + best-available context.
 * - created, modified: model-only; None for dataclass instances.
 */
export type Notification = {
    readonly id: string;
    readonly title: string;
    readonly notification_type: string;
    readonly status: string;
    /**
     * Render the body template with the best-available context.
     *
     * Priority:
     * 1. context_used — the context that was recorded at send time (on model rows,
     * set by the backend when the notification was processed).
     * 2. context_kwargs — the original kwargs passed at creation time.
     * 3. Empty dict — render the template with no context (graceful degradation).
     *
     * Returns an empty string on rendering failure so the response always serialises.
     */
    readonly body: string;
    /**
     * Return the creation timestamp as ISO 8601 string, or None for dataclasses.
     *
     * The vintasend Notification dataclass has no `created` attribute; only the
     * vintasend_django ORM model does.
     */
    readonly created: string | null;
    /**
     * Return the last-modified timestamp as ISO 8601 string, or None for dataclasses.
     *
     * In vintasend 1.2.0 the Notification dataclass carries `modified`; ORM rows also
     * have it. Returns None when the attribute is absent or None.
     */
    readonly modified: string | null;
};

/**
 * Serializer for Organization instances.
 *
 * The ``google_service_account`` field supports both reading and writing:
 * - **Write**: accepts ``email``, ``admin_email``,
 * ``private_key_id`` (write-only), and ``private_key`` (write-only).
 * Omitting the field on PATCH leaves existing credentials unchanged.
 * - **Read**: returns ``email``, ``admin_email``, and ``configured: true/false``.
 * Secret fields are never returned.
 */
export type Organization = {
    readonly id: number;
    name: string;
    /**
     * Whether to sync rooms for this organization.
     */
    should_sync_rooms?: boolean;
    /**
     * Return read-only service account info (no secrets), or None if unconfigured.
     */
    readonly google_service_account: {
        [key: string]: unknown;
    } | null;
    readonly created: string;
    readonly modified: string;
};

/**
 * Serializer for OrganizationBranding (reseller-admin REST endpoints).
 *
 * Exposes app_name, logo_url, primary_color, secondary_color, support_email,
 * and return_url_allowlist. NEVER exposes can_invite_organizations or makes
 * organization writable (the org is set from the acting org in the view).
 *
 * Validates:
 * - Color format: #RRGGBB or #RRGGBBAA (regex).
 * - Each return_url_allowlist entry is a valid URL (URLValidator).
 */
export type OrganizationBranding = {
    /**
     * The display name of the white-labeled app (e.g., 'MyScheduler').
     */
    app_name: string;
    /**
     * URL to the reseller's logo image.
     */
    logo_url?: string;
    /**
     * Primary color as hex code: #RRGGBB or #RRGGBBAA.
     */
    primary_color?: string;
    /**
     * Secondary color as hex code: #RRGGBB or #RRGGBBAA.
     */
    secondary_color?: string;
    /**
     * Email address for the From/reply-to on branded transactional emails.
     */
    support_email?: string;
    /**
     * List of URLs that are allowed as return addresses after OAuth flows.
     */
    return_url_allowlist?: Array<string>;
};

/**
 * Lightweight read-only serializer for an Organization.
 *
 * Exposes only the fields needed for the org-switcher list: ``id`` and ``name``.
 * Intentionally avoids the heavier ``OrganizationSerializer`` (which loads the
 * Google service account) to keep ``GET /organizations/mine/`` fast.
 */
export type OrganizationBrief = {
    readonly id: number;
    readonly name: string;
};

/**
 * Serializer for managing OrganizationInvitation instances.
 */
export type OrganizationInvitation = {
    readonly id: number;
    email: string;
    first_name?: string;
    last_name?: string;
    readonly organization: number;
    readonly invited_by: number | null;
    readonly accepted_at: string | null;
    readonly expires_at: string;
    readonly created: string;
    readonly modified: string;
};

/**
 * Read-only serializer for listing and retrieving organization members.
 *
 * Exposes membership role, active status, and flattened user information
 * (email, first_name, last_name) for the admin datatable.
 */
export type OrganizationMembership = {
    readonly user_id: number;
    readonly organization_id: number;
    /**
     * Role the user holds in this organization. Admins can manage organization-scoped resources (e.g. CalendarGroups) regardless of direct ownership.
     *
     * * `member` - Member
     * * `admin` - Admin
     */
    role: RoleEnum;
    /**
     * Whether this membership is active. Inactive memberships are treated as gated: the user still has a row but loses all tenant-scoped access until reactivated. Use this to disable a user without deleting their membership record (which would lose role/history). Default True keeps every existing read unchanged.
     */
    readonly is_active: boolean;
    readonly user_email: string;
    readonly user_first_name: string;
    readonly user_last_name: string;
};

/**
 * Membership identity for a calendar owner.
 *
 * A membership has no scalar id (it is identified by the ``(user_id,
 * organization_id)`` pair), so the representation exposes that pair plus the
 * membership ``role``.
 */
export type OwnershipMembership = {
    readonly user_id: number;
    readonly organization_id: number;
    readonly role: string;
};

export type PaginatedAvailableTimeList = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<AvailableTime>;
};

export type PaginatedBlockedTimeList = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<BlockedTime>;
};

export type PaginatedBookableSlotProposalList = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<BookableSlotProposal>;
};

export type PaginatedCalendarEventList = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<CalendarEvent>;
};

export type PaginatedCalendarGroupList = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<CalendarGroup>;
};

export type PaginatedCalendarGroupRangeAvailabilityList = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<CalendarGroupRangeAvailability>;
};

export type PaginatedCalendarList = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<Calendar>;
};

export type PaginatedOrganizationInvitationList = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<OrganizationInvitation>;
};

export type PaginatedOrganizationMembershipList = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<OrganizationMembership>;
};

export type PaginatedServiceAccountReadList = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<ServiceAccountRead>;
};

export type PaginatedSystemUserTokenList = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<SystemUserToken>;
};

export type PaginatedWebhookConfigurationList = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<WebhookConfiguration>;
};

export type PaginatedWebhookEventList = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<WebhookEvent>;
};

export type ParentEvent = {
    readonly id: number;
    title: string;
    readonly external_id: string;
    readonly start_time: string;
    readonly end_time: string;
    readonly created: string;
    readonly modified: string;
};

/**
 * Serializer for AvailableTime model with recurring support.
 */
export type PatchedAvailableTime = {
    readonly id?: number;
    start_time?: string;
    end_time?: string;
    timezone?: string;
    /**
     * Recurrence rule data for creating recurring available times
     */
    recurrence_rule?: RecurrenceRule;
    /**
     * True if this is an instance of a recurring available time
     */
    readonly is_recurring_instance?: boolean;
    /**
     * True if this is a recurring available time
     */
    readonly is_recurring?: boolean;
    /**
     * Get parent available time for instances.
     */
    readonly parent_available_time?: {
        id: number;
    } | null;
    /**
     * For recurring instances, this identifies which occurrence this is
     */
    readonly recurrence_id?: string | null;
    readonly created?: string;
    readonly modified?: string;
    calendar?: number | null;
};

/**
 * Serializer for BillingProfile virtual model.
 */
export type PatchedBillingProfile = {
    readonly id?: number;
    document_type?: string;
    document_number?: string;
    billing_address?: BillingAddress;
    readonly created?: string;
    readonly modified?: string;
};

/**
 * Serializer for BlockedTime model with recurring support.
 */
export type PatchedBlockedTime = {
    readonly id?: number;
    start_time?: string;
    end_time?: string;
    timezone?: string;
    reason?: string;
    /**
     * Recurrence rule data for creating recurring blocked times
     */
    recurrence_rule?: RecurrenceRule;
    readonly external_id?: string;
    /**
     * True if this is an instance of a recurring blocked time
     */
    readonly is_recurring_instance?: boolean;
    /**
     * True if this is a recurring blocked time
     */
    readonly is_recurring?: boolean;
    /**
     * Get parent blocked time for instances.
     */
    readonly parent_blocked_time?: {
        id: number;
        reason: string | null;
    } | null;
    readonly created?: string;
    readonly modified?: string;
    calendar?: number | null;
};

export type PatchedCalendar = {
    readonly id?: number;
    name?: string;
    description?: string;
    readonly email?: string;
    readonly external_id?: string;
    provider?: ProviderEnum;
    /**
     * The type of calendar. Personal calendars are for individual use, resource calendars are for shared resources, and virtual calendars are for online meetings or events.
     *
     * * `personal` - Personal Calendar
     * * `resource` - Resource Calendar
     * * `virtual` - Virtual Calendar
     * * `bundle` - Bundle Calendar
     */
    calendar_type?: CalendarTypeEnum;
    /**
     * The maximum number of attendees that can be accommodated in this calendar's events. This is only applicable for resource calendars.
     */
    capacity?: number | null;
    /**
     * If true, this calendar can manage its own available time windows. If not, it will use the available time windows of the external calendar it's attached to.
     */
    manage_available_windows?: boolean;
    /**
     * Controls how this calendar appears in queries. active: listed and available for booking (default). unlisted: hidden from listing/booking queries but still synced for conflict detection; survives re-import so user opt-out is preserved. inactive: soft-deleted, hidden from all queries and not synced. Use DELETE /calendars/{id}/ to transition to inactive instead of hard-deleting.
     *
     * * `active` - Active
     * * `unlisted` - Unlisted
     * * `inactive` - Inactive
     */
    visibility?: VisibilityEnum;
    /**
     * Whether this calendar's events are pulled from the external provider. Set to False to skip syncing calendars that aren't useful for scheduling — holidays, birthdays, organization-wide events, etc. When False, no new CalendarSync is requested for this calendar (including webhook- and import-triggered syncs); previously synced events are left untouched. Default True keeps existing calendars syncing as before.
     */
    sync_enabled?: boolean;
};

/**
 * Serializer for updating a bundle calendar's child calendars and primary calendar.
 */
export type PatchedCalendarBundleUpdate = {
    bundle_calendars?: Array<number>;
    primary_calendar?: number | null;
};

export type PatchedCalendarEvent = {
    readonly id?: number;
    title?: string;
    description?: string;
    start_time?: string;
    end_time?: string;
    timezone?: string;
    readonly created?: string;
    readonly modified?: string;
    readonly external_id?: string;
    external_attendances?: Array<EventExternalAttendance>;
    attendances?: Array<EventAttendance>;
    resource_allocations?: Array<ResourceAllocation>;
    /**
     * Recurrence rule data for creating recurring events
     */
    recurrence_rule?: RecurrenceRule;
    parent_recurring_object?: ParentEvent;
    /**
     * True if this is an instance of a recurring event
     */
    readonly is_recurring_instance?: boolean;
    /**
     * True if this is a recurring event
     */
    readonly is_recurring?: boolean;
    /**
     * True if this object is an exception to the recurrence rule (modified occurrence)
     */
    is_recurring_exception?: boolean;
    /**
     * For recurring instances, this identifies which occurrence this is
     */
    recurrence_id?: string | null;
};

export type PatchedCalendarGroup = {
    readonly id?: number;
    name?: string;
    description?: string;
    slots?: Array<CalendarGroupSlot>;
    readonly created?: string;
    readonly modified?: string;
};

/**
 * Serializer for Organization instances.
 *
 * The ``google_service_account`` field supports both reading and writing:
 * - **Write**: accepts ``email``, ``admin_email``,
 * ``private_key_id`` (write-only), and ``private_key`` (write-only).
 * Omitting the field on PATCH leaves existing credentials unchanged.
 * - **Read**: returns ``email``, ``admin_email``, and ``configured: true/false``.
 * Secret fields are never returned.
 */
export type PatchedOrganization = {
    readonly id?: number;
    name?: string;
    /**
     * Whether to sync rooms for this organization.
     */
    should_sync_rooms?: boolean;
    /**
     * Return read-only service account info (no secrets), or None if unconfigured.
     */
    readonly google_service_account?: {
        [key: string]: unknown;
    } | null;
    readonly created?: string;
    readonly modified?: string;
};

/**
 * Serializer for OrganizationBranding (reseller-admin REST endpoints).
 *
 * Exposes app_name, logo_url, primary_color, secondary_color, support_email,
 * and return_url_allowlist. NEVER exposes can_invite_organizations or makes
 * organization writable (the org is set from the acting org in the view).
 *
 * Validates:
 * - Color format: #RRGGBB or #RRGGBBAA (regex).
 * - Each return_url_allowlist entry is a valid URL (URLValidator).
 */
export type PatchedOrganizationBranding = {
    /**
     * The display name of the white-labeled app (e.g., 'MyScheduler').
     */
    app_name?: string;
    /**
     * URL to the reseller's logo image.
     */
    logo_url?: string;
    /**
     * Primary color as hex code: #RRGGBB or #RRGGBBAA.
     */
    primary_color?: string;
    /**
     * Secondary color as hex code: #RRGGBB or #RRGGBBAA.
     */
    secondary_color?: string;
    /**
     * Email address for the From/reply-to on branded transactional emails.
     */
    support_email?: string;
    /**
     * List of URLs that are allowed as return addresses after OAuth flows.
     */
    return_url_allowlist?: Array<string>;
};

export type PatchedProfile = {
    readonly id?: number;
    first_name?: string;
    last_name?: string;
    profile_picture?: string | null;
};

/**
 * Write serializer for creating/rotating the org-level service account.
 *
 * ``private_key`` and ``private_key_id`` are write-only and are never echoed
 * back in any response (reads go through ``ServiceAccountReadSerializer``).
 */
export type PatchedServiceAccountWrite = {
    email?: string;
    /**
     * Google Workspace super-admin email used as the DWD impersonation subject. The service account must have domain-wide delegation granted for the Admin SDK and Calendar API scopes in the Google Admin Console.
     */
    admin_email?: string;
};

/**
 * Input serializer for updating a public-API token's resource grants (Phase 15).
 *
 * Accepts ``available_resources`` (a non-empty list of valid ``PublicAPIResources`` values)
 * only.  ``integration_name`` and ``token`` are never accepted or changed.
 * The view reconciles ResourceAccess rows: adds rows for newly-granted resources,
 * removes rows for dropped resources.
 */
export type PatchedSystemUserTokenUpdate = {
    available_resources?: Array<AvailableResourcesEnum>;
};

export type PatchedWebhookConfiguration = {
    readonly id?: number;
    event_type?: EventTypeEnum;
    url?: string;
    headers?: unknown;
};

export type Profile = {
    readonly id: number;
    first_name?: string;
    last_name?: string;
    profile_picture?: string | null;
};

export type ProfilePictureUploadParams = {
    object_key: string;
    access_key_id: string | null;
    session_token: string | null;
    region: string;
    bucket: string;
    endpoint: string;
    acl: string;
    allow_existence_optimization: boolean;
};

export type ProfilePictureUploadParamsRequest = {
    file_name: string;
    file_type: string;
    file_size: number;
};

/**
 * * `internal` - Internal Calendar
 * * `google` - Google Calendar
 * * `microsoft` - Microsoft Outlook Calendar
 * * `apple` - Apple Calendar
 * * `ics` - ICS
 */
export type ProviderEnum = 'internal' | 'google' | 'microsoft' | 'apple' | 'ics';

/**
 * * `accepted` - Accepted
 * * `declined` - Declined
 * * `pending` - Pending
 */
export type RsvpStatusEnum = 'accepted' | 'declined' | 'pending';

export type RecurrenceRule = {
    readonly id: number;
    /**
     * How often the event repeats (DAILY, WEEKLY, MONTHLY, YEARLY)
     *
     * * `DAILY` - Daily
     * * `WEEKLY` - Weekly
     * * `MONTHLY` - Monthly
     * * `YEARLY` - Yearly
     */
    frequency: FrequencyEnum;
    /**
     * The interval between each frequency iteration (e.g., every 2 weeks)
     */
    interval?: number;
    /**
     * Number of occurrences after which the recurrence ends
     */
    count?: number | null;
    /**
     * The date and time until which the recurrence is valid
     */
    until?: string | null;
    /**
     * Comma-separated list of weekdays (e.g., 'MO,WE,FR')
     */
    by_weekday?: string;
    /**
     * Comma-separated list of month days (e.g., '1,15,-1' for 1st, 15th, last day)
     */
    by_month_day?: string;
    /**
     * Comma-separated list of months (1-12)
     */
    by_month?: string;
    /**
     * Comma-separated list of year days (1-366 or -366 to -1)
     */
    by_year_day?: string;
    /**
     * Comma-separated list of week numbers (1-53 or -53 to -1)
     */
    by_week_number?: string;
    /**
     * Comma-separated list of hours (0-23)
     */
    by_hour?: string;
    /**
     * Comma-separated list of minutes (0-59)
     */
    by_minute?: string;
    /**
     * Comma-separated list of seconds (0-59)
     */
    by_second?: string;
    /**
     * First day of the week
     *
     * * `MO` - Monday
     * * `TU` - Tuesday
     * * `WE` - Wednesday
     * * `TH` - Thursday
     * * `FR` - Friday
     * * `SA` - Saturday
     * * `SU` - Sunday
     */
    week_start?: WeekStartEnum;
    readonly rrule_string: string;
    readonly created: string;
    readonly modified: string;
};

export type ResourceAllocation = {
    /**
     * ID of the external attendee.
     */
    id?: number | null;
    calendar: number;
    status: RsvpStatusEnum;
    readonly created: string;
    readonly modified: string;
};

/**
 * Create an internal (manual) resource calendar. Admin-gated at the view layer.
 */
export type ResourceCalendarCreate = {
    name: string;
    description?: string;
    /**
     * The maximum number of attendees that can be accommodated in this calendar's events. This is only applicable for resource calendars.
     */
    capacity?: number | null;
    /**
     * If true, this calendar can manage its own available time windows. If not, it will use the available time windows of the external calendar it's attached to.
     */
    manage_available_windows?: boolean;
};

/**
 * * `member` - Member
 * * `admin` - Admin
 */
export type RoleEnum = 'member' | 'admin';

/**
 * Read serializer for the org-level Google Calendar service account (CRUD surface).
 *
 * Exposes only non-secret fields plus a ``configured`` flag. ``private_key``
 * and ``private_key_id`` are never returned.
 */
export type ServiceAccountRead = {
    readonly id: number;
    readonly email: string;
    /**
     * Google Workspace super-admin email used as the DWD impersonation subject. The service account must have domain-wide delegation granted for the Admin SDK and Calendar API scopes in the Google Admin Console.
     */
    readonly admin_email: string;
    /**
     * A persisted row is, by definition, configured.
     */
    readonly configured: boolean;
    readonly created: string;
    readonly modified: string;
};

/**
 * Write serializer for creating/rotating the org-level service account.
 *
 * ``private_key`` and ``private_key_id`` are write-only and are never echoed
 * back in any response (reads go through ``ServiceAccountReadSerializer``).
 */
export type ServiceAccountWrite = {
    email: string;
    /**
     * Google Workspace super-admin email used as the DWD impersonation subject. The service account must have domain-wide delegation granted for the Admin SDK and Calendar API scopes in the Google Admin Console.
     */
    admin_email?: string;
};

/**
 * Read-only serializer for listing and retrieving public-API tokens.
 *
 * Exposes ``id``, ``integration_name``, ``is_active``, ``available_resources``
 * (list of resource_name strings from the related ``ResourceAccess`` rows), and
 * ``scoped_to_user`` (the owner's User id from the denormalized membership column, null
 * for org-wide tokens).  The REST field name ``scoped_to_user`` is kept for API stability;
 * the value is the denormalized ``scoped_to_membership_user_id``.
 * Never exposes ``long_lived_token_hash`` or ``token``.
 *
 * Optimized for list queries: uses prefetched ``available_resources`` from the viewset's
 * ``get_queryset`` to avoid N+1 queries.  ``scoped_to_membership_user_id`` is a concrete
 * column on the row, so it needs no join.
 */
export type SystemUserToken = {
    readonly id: number;
    readonly integration_name: string;
    /**
     * Indicates if the user is active.
     */
    readonly is_active: boolean;
    /**
     * Return a list of resource_name values from the prefetched ResourceAccess rows.
     */
    readonly available_resources: Array<string>;
    /**
     * Return the owner's User id from the denormalized membership column, or None.
     *
     * ``scoped_to_membership_user_id`` is a concrete column already storing the
     * membership's user_id, so the value is returned directly with no extra query.
     */
    readonly scoped_to_user: number | null;
};

/**
 * Input serializer for creating a new public-API token (SystemUser + ResourceAccess rows).
 *
 * Accepts ``integration_name``, ``available_resources`` (a non-empty list of valid
 * ``PublicAPIResources`` values), and an optional ``scoped_to_user`` (internal User id).
 * ``create()`` provisions the ``SystemUser`` via the injected auth service and
 * bulk-creates the ``ResourceAccess`` grants, attaching the write-once plaintext
 * ``token`` to the returned instance.  Never stores or exposes ``long_lived_token_hash``.
 *
 * When ``scoped_to_user`` is supplied and non-null:
 * - The referenced user must be an active member of the caller's organisation.
 * - ``available_resources`` must be a subset of ``PROVIDER_SCOPED_RESOURCES``.
 * When ``scoped_to_user`` is absent or null, behaviour is exactly as before:
 * any valid ``PublicAPIResources`` value is accepted and the token is org-wide.
 */
export type SystemUserTokenCreate = {
    integration_name: string;
    available_resources: Array<AvailableResourcesEnum>;
    scoped_to_user?: number | null;
};

/**
 * Read serializer for the created SystemUser.
 *
 * Includes the write-once ``token`` field (sourced from the view) and
 * ``available_resources`` (derived from the related ``ResourceAccess`` rows).
 * Exposes ``scoped_to_user`` as the owner's User id derived from the stored membership
 * reference (null for org-wide tokens).  The REST field name ``scoped_to_user`` is kept
 * for API stability; the value is the denormalized ``scoped_to_membership_user_id``.
 * Never exposes ``long_lived_token_hash``.
 */
export type SystemUserTokenResponse = {
    readonly id: number;
    readonly integration_name: string;
    /**
     * Indicates if the user is active.
     */
    readonly is_active: boolean;
    /**
     * Return a list of resource_name values from the related ResourceAccess rows.
     */
    readonly available_resources: Array<string>;
    /**
     * Return the owner's User id from the denormalized membership column, or None.
     *
     * ``scoped_to_membership_user_id`` already stores the membership's user_id, so the
     * value is returned directly with no extra query.
     */
    readonly scoped_to_user: number | null;
    readonly token: string;
};

/**
 * Input serializer for updating a public-API token's resource grants (Phase 15).
 *
 * Accepts ``available_resources`` (a non-empty list of valid ``PublicAPIResources`` values)
 * only.  ``integration_name`` and ``token`` are never accepted or changed.
 * The view reconciles ResourceAccess rows: adds rows for newly-granted resources,
 * removes rows for dropped resources.
 */
export type SystemUserTokenUpdate = {
    available_resources: Array<AvailableResourcesEnum>;
};

/**
 * * `import` - Import
 * * `manual` - Manual
 * * `webhook` - Webhook
 * * `admin` - Admin
 */
export type TriggerSourceEnum = 'import' | 'manual' | 'webhook' | 'admin';

export type UnavailableTimeWindow = {
    id: number;
    reason: string;
    start_time: string;
    end_time: string;
    readonly reason_description: string;
};

/**
 * Request serializer for updating an organization member's role.
 */
export type UpdateMembershipRole = {
    role: RoleEnum;
};

/**
 * * `active` - Active
 * * `unlisted` - Unlisted
 * * `inactive` - Inactive
 */
export type VisibilityEnum = 'active' | 'unlisted' | 'inactive';

export type WebhookConfiguration = {
    readonly id: number;
    event_type?: EventTypeEnum;
    url: string;
    headers?: unknown;
};

export type WebhookEvent = {
    readonly configuration: number;
    readonly main_event: number;
    event_type: EventTypeEnum;
    url: string;
    status: WebhookEventStatusEnum;
    headers?: unknown;
    payload: unknown;
    readonly response_status: number | null;
    readonly response_body: unknown;
    readonly response_headers: unknown;
    readonly retry_number: number | null;
    readonly send_after: string | null;
    readonly created: string;
    readonly modified: string;
};

/**
 * * `pending` - Pending
 * * `success` - Success
 * * `failed` - Failed
 */
export type WebhookEventStatusEnum = 'pending' | 'success' | 'failed';

/**
 * * `MO` - Monday
 * * `TU` - Tuesday
 * * `WE` - Wednesday
 * * `TH` - Thursday
 * * `FR` - Friday
 * * `SA` - Saturday
 * * `SU` - Sunday
 */
export type WeekStartEnum = 'MO' | 'TU' | 'WE' | 'TH' | 'FR' | 'SA' | 'SU';

export type CalendarGroupSlotSelectionInput = {
    slot_id: number;
    calendar_ids: Array<number>;
};

export type RangeInput = {
    start_time: string;
    end_time: string;
};

/**
 * Serializer for AvailableTime model with recurring support.
 */
export type AvailableTimeWritable = {
    start_time: string;
    end_time: string;
    timezone: string;
    /**
     * Recurrence rule data for creating recurring available times
     */
    recurrence_rule?: RecurrenceRuleWritable;
    /**
     * RRULE string for creating recurring available times
     */
    rrule_string?: string;
    calendar?: number | null;
};

export type AvailableTimeBulkModificationWritable = {
    modification_start_date: string;
    /**
     * Recurrence rule data for the modification range
     */
    recurrence_rule?: RecurrenceRuleWritable;
    /**
     * RRULE string for the modification range
     */
    rrule_string?: string | null;
    modified_start_time_offset?: string | null;
    modified_end_time_offset?: string | null;
    is_cancelled?: boolean;
};

/**
 * Serializer for BillingAddress virtual model.
 */
export type BillingAddressWritable = {
    street_name: string;
    street_number: string;
    neighborhood?: string;
    address_line_2?: string;
    city: string;
    state: string;
    country: string;
    zip_code: string;
};

/**
 * Serializer for BillingProfile virtual model.
 */
export type BillingProfileWritable = {
    document_type: string;
    document_number: string;
    billing_address: BillingAddressWritable;
};

/**
 * Serializer for BlockedTime model with recurring support.
 */
export type BlockedTimeWritable = {
    start_time: string;
    end_time: string;
    timezone: string;
    reason?: string;
    /**
     * Recurrence rule data for creating recurring blocked times
     */
    recurrence_rule?: RecurrenceRuleWritable;
    /**
     * RRULE string for creating recurring blocked times
     */
    rrule_string?: string;
    calendar?: number | null;
};

export type BlockedTimeBulkModificationWritable = {
    modification_start_date: string;
    modified_reason?: string | null;
    /**
     * Recurrence rule data for the modification range
     */
    recurrence_rule?: RecurrenceRuleWritable;
    /**
     * RRULE string for the modification range
     */
    rrule_string?: string | null;
    modified_start_time_offset?: string | null;
    modified_end_time_offset?: string | null;
    is_cancelled?: boolean;
};

/**
 * Serializer for creating multiple blocked times.
 */
export type BulkBlockedTimeWritable = {
    blocked_times: Array<BlockedTimeWritable>;
};

export type CalendarWritable = {
    name: string;
    description?: string;
    /**
     * The maximum number of attendees that can be accommodated in this calendar's events. This is only applicable for resource calendars.
     */
    capacity?: number | null;
    /**
     * If true, this calendar can manage its own available time windows. If not, it will use the available time windows of the external calendar it's attached to.
     */
    manage_available_windows?: boolean;
    /**
     * Controls how this calendar appears in queries. active: listed and available for booking (default). unlisted: hidden from listing/booking queries but still synced for conflict detection; survives re-import so user opt-out is preserved. inactive: soft-deleted, hidden from all queries and not synced. Use DELETE /calendars/{id}/ to transition to inactive instead of hard-deleting.
     *
     * * `active` - Active
     * * `unlisted` - Unlisted
     * * `inactive` - Inactive
     */
    visibility?: VisibilityEnum;
    /**
     * Whether this calendar's events are pulled from the external provider. Set to False to skip syncing calendars that aren't useful for scheduling — holidays, birthdays, organization-wide events, etc. When False, no new CalendarSync is requested for this calendar (including webhook- and import-triggered syncs); previously synced events are left untouched. Default True keeps existing calendars syncing as before.
     */
    sync_enabled?: boolean;
};

export type CalendarEventWritable = {
    provider?: string;
    title: string;
    description?: string;
    start_time: string;
    end_time: string;
    timezone: string;
    external_attendances: Array<EventExternalAttendanceWritable>;
    attendances: Array<EventAttendanceWritable>;
    resource_allocations: Array<ResourceAllocationWritable>;
    /**
     * Recurrence rule data for creating recurring events
     */
    recurrence_rule?: RecurrenceRuleWritable;
    /**
     * RRULE string for creating recurring events
     */
    rrule_string?: string;
    /**
     * ID of parent event for recurring instances
     */
    parent_recurring_object_id?: number;
    /**
     * True if this object is an exception to the recurrence rule (modified occurrence)
     */
    is_recurring_exception?: boolean;
    /**
     * For recurring instances, this identifies which occurrence this is
     */
    recurrence_id?: string | null;
    google_calendar_service_account?: number;
    calendar?: number;
};

export type CalendarGroupWritable = {
    name: string;
    description?: string;
    slots: Array<CalendarGroupSlotWritable>;
};

export type CalendarGroupRangeAvailabilityWritable = {
    start_time: string;
    end_time: string;
    slots: Array<CalendarGroupSlotAvailabilityWritable>;
};

/**
 * Nested slot representation used inside CalendarGroupSerializer.
 *
 * On write, accepts `calendar_ids: list[int]`; on read exposes the calendar
 * pool via `calendars` (the M2M). We deliberately keep slot writes to
 * payload-time data only — persistence happens through
 * `CalendarGroupSerializer` which delegates to `CalendarGroupService`.
 */
export type CalendarGroupSlotWritable = {
    name: string;
    description?: string;
    order?: number;
    /**
     * How many calendars from the pool must be selected when booking. Default 1; use larger values when a slot needs multiple calendars (e.g. two nurses).
     */
    required_count?: number;
    calendar_ids: Array<number>;
};

export type CalendarGroupSlotAvailabilityWritable = {
    slot_id: number;
    available_calendar_ids: Array<number>;
    required_count: number;
};

export type CalendarSyncWritable = {
    start_datetime: string;
    end_datetime: string;
    should_update_events: boolean;
};

export type EventAttendanceWritable = {
    /**
     * ID of the external attendee.
     */
    id?: number | null;
    user_id: number;
};

/**
 * Serializer for creating bulk modifications on recurring events from a given date.
 */
export type EventBulkModificationWritable = {
    modification_start_date: string;
    modified_title?: string | null;
    modified_description?: string | null;
    /**
     * Recurrence rule data for the modification range
     */
    recurrence_rule?: RecurrenceRuleWritable;
    /**
     * RRULE string for the modification range
     */
    rrule_string?: string | null;
    modified_start_time_offset?: string | null;
    modified_end_time_offset?: string | null;
    is_cancelled?: boolean;
};

export type EventExternalAttendanceWritable = {
    /**
     * ID of the external attendee.
     */
    id?: number | null;
    external_attendee: ExternalAttendeeWritable;
};

export type ExternalAttendeeWritable = {
    /**
     * ID of the external attendee.
     */
    id?: number | null;
    name?: string;
    email: string;
};

/**
 * Serializer for Organization instances.
 *
 * The ``google_service_account`` field supports both reading and writing:
 * - **Write**: accepts ``email``, ``admin_email``,
 * ``private_key_id`` (write-only), and ``private_key`` (write-only).
 * Omitting the field on PATCH leaves existing credentials unchanged.
 * - **Read**: returns ``email``, ``admin_email``, and ``configured: true/false``.
 * Secret fields are never returned.
 */
export type OrganizationWritable = {
    name: string;
    /**
     * Whether to sync rooms for this organization.
     */
    should_sync_rooms?: boolean;
};

/**
 * Serializer for managing OrganizationInvitation instances.
 */
export type OrganizationInvitationWritable = {
    email: string;
    first_name?: string;
    last_name?: string;
};

export type PaginatedAvailableTimeListWritable = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<AvailableTimeWritable>;
};

export type PaginatedBlockedTimeListWritable = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<BlockedTimeWritable>;
};

export type PaginatedCalendarEventListWritable = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<CalendarEventWritable>;
};

export type PaginatedCalendarGroupListWritable = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<CalendarGroupWritable>;
};

export type PaginatedCalendarGroupRangeAvailabilityListWritable = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<CalendarGroupRangeAvailabilityWritable>;
};

export type PaginatedCalendarListWritable = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<CalendarWritable>;
};

export type PaginatedOrganizationInvitationListWritable = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<OrganizationInvitationWritable>;
};

export type PaginatedOrganizationMembershipListWritable = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<unknown>;
};

export type PaginatedServiceAccountReadListWritable = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<unknown>;
};

export type PaginatedSystemUserTokenListWritable = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<unknown>;
};

export type PaginatedWebhookConfigurationListWritable = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<WebhookConfigurationWritable>;
};

export type PaginatedWebhookEventListWritable = {
    count: number;
    next?: string | null;
    previous?: string | null;
    results: Array<WebhookEventWritable>;
};

export type ParentEventWritable = {
    title: string;
};

/**
 * Serializer for AvailableTime model with recurring support.
 */
export type PatchedAvailableTimeWritable = {
    start_time?: string;
    end_time?: string;
    timezone?: string;
    /**
     * Recurrence rule data for creating recurring available times
     */
    recurrence_rule?: RecurrenceRuleWritable;
    /**
     * RRULE string for creating recurring available times
     */
    rrule_string?: string;
    calendar?: number | null;
};

/**
 * Serializer for BillingProfile virtual model.
 */
export type PatchedBillingProfileWritable = {
    document_type?: string;
    document_number?: string;
    billing_address?: BillingAddressWritable;
};

/**
 * Serializer for BlockedTime model with recurring support.
 */
export type PatchedBlockedTimeWritable = {
    start_time?: string;
    end_time?: string;
    timezone?: string;
    reason?: string;
    /**
     * Recurrence rule data for creating recurring blocked times
     */
    recurrence_rule?: RecurrenceRuleWritable;
    /**
     * RRULE string for creating recurring blocked times
     */
    rrule_string?: string;
    calendar?: number | null;
};

export type PatchedCalendarWritable = {
    name?: string;
    description?: string;
    /**
     * The maximum number of attendees that can be accommodated in this calendar's events. This is only applicable for resource calendars.
     */
    capacity?: number | null;
    /**
     * If true, this calendar can manage its own available time windows. If not, it will use the available time windows of the external calendar it's attached to.
     */
    manage_available_windows?: boolean;
    /**
     * Controls how this calendar appears in queries. active: listed and available for booking (default). unlisted: hidden from listing/booking queries but still synced for conflict detection; survives re-import so user opt-out is preserved. inactive: soft-deleted, hidden from all queries and not synced. Use DELETE /calendars/{id}/ to transition to inactive instead of hard-deleting.
     *
     * * `active` - Active
     * * `unlisted` - Unlisted
     * * `inactive` - Inactive
     */
    visibility?: VisibilityEnum;
    /**
     * Whether this calendar's events are pulled from the external provider. Set to False to skip syncing calendars that aren't useful for scheduling — holidays, birthdays, organization-wide events, etc. When False, no new CalendarSync is requested for this calendar (including webhook- and import-triggered syncs); previously synced events are left untouched. Default True keeps existing calendars syncing as before.
     */
    sync_enabled?: boolean;
};

export type PatchedCalendarEventWritable = {
    provider?: string;
    title?: string;
    description?: string;
    start_time?: string;
    end_time?: string;
    timezone?: string;
    external_attendances?: Array<EventExternalAttendanceWritable>;
    attendances?: Array<EventAttendanceWritable>;
    resource_allocations?: Array<ResourceAllocationWritable>;
    /**
     * Recurrence rule data for creating recurring events
     */
    recurrence_rule?: RecurrenceRuleWritable;
    /**
     * RRULE string for creating recurring events
     */
    rrule_string?: string;
    /**
     * ID of parent event for recurring instances
     */
    parent_recurring_object_id?: number;
    /**
     * True if this object is an exception to the recurrence rule (modified occurrence)
     */
    is_recurring_exception?: boolean;
    /**
     * For recurring instances, this identifies which occurrence this is
     */
    recurrence_id?: string | null;
    google_calendar_service_account?: number;
    calendar?: number;
};

export type PatchedCalendarGroupWritable = {
    name?: string;
    description?: string;
    slots?: Array<CalendarGroupSlotWritable>;
};

/**
 * Serializer for Organization instances.
 *
 * The ``google_service_account`` field supports both reading and writing:
 * - **Write**: accepts ``email``, ``admin_email``,
 * ``private_key_id`` (write-only), and ``private_key`` (write-only).
 * Omitting the field on PATCH leaves existing credentials unchanged.
 * - **Read**: returns ``email``, ``admin_email``, and ``configured: true/false``.
 * Secret fields are never returned.
 */
export type PatchedOrganizationWritable = {
    name?: string;
    /**
     * Whether to sync rooms for this organization.
     */
    should_sync_rooms?: boolean;
};

export type PatchedProfileWritable = {
    first_name?: string;
    last_name?: string;
    profile_picture?: string | null;
};

/**
 * Write serializer for creating/rotating the org-level service account.
 *
 * ``private_key`` and ``private_key_id`` are write-only and are never echoed
 * back in any response (reads go through ``ServiceAccountReadSerializer``).
 */
export type PatchedServiceAccountWriteWritable = {
    email?: string;
    /**
     * Google Workspace super-admin email used as the DWD impersonation subject. The service account must have domain-wide delegation granted for the Admin SDK and Calendar API scopes in the Google Admin Console.
     */
    admin_email?: string;
    private_key_id?: string;
    private_key?: string;
};

export type PatchedWebhookConfigurationWritable = {
    event_type?: EventTypeEnum;
    url?: string;
    headers?: unknown;
};

export type ProfileWritable = {
    first_name?: string;
    last_name?: string;
    profile_picture?: string | null;
};

export type RecurrenceRuleWritable = {
    /**
     * How often the event repeats (DAILY, WEEKLY, MONTHLY, YEARLY)
     *
     * * `DAILY` - Daily
     * * `WEEKLY` - Weekly
     * * `MONTHLY` - Monthly
     * * `YEARLY` - Yearly
     */
    frequency: FrequencyEnum;
    /**
     * The interval between each frequency iteration (e.g., every 2 weeks)
     */
    interval?: number;
    /**
     * Number of occurrences after which the recurrence ends
     */
    count?: number | null;
    /**
     * The date and time until which the recurrence is valid
     */
    until?: string | null;
    /**
     * Comma-separated list of weekdays (e.g., 'MO,WE,FR')
     */
    by_weekday?: string;
    /**
     * Comma-separated list of month days (e.g., '1,15,-1' for 1st, 15th, last day)
     */
    by_month_day?: string;
    /**
     * Comma-separated list of months (1-12)
     */
    by_month?: string;
    /**
     * Comma-separated list of year days (1-366 or -366 to -1)
     */
    by_year_day?: string;
    /**
     * Comma-separated list of week numbers (1-53 or -53 to -1)
     */
    by_week_number?: string;
    /**
     * Comma-separated list of hours (0-23)
     */
    by_hour?: string;
    /**
     * Comma-separated list of minutes (0-59)
     */
    by_minute?: string;
    /**
     * Comma-separated list of seconds (0-59)
     */
    by_second?: string;
    /**
     * First day of the week
     *
     * * `MO` - Monday
     * * `TU` - Tuesday
     * * `WE` - Wednesday
     * * `TH` - Thursday
     * * `FR` - Friday
     * * `SA` - Saturday
     * * `SU` - Sunday
     */
    week_start?: WeekStartEnum;
};

export type ResourceAllocationWritable = {
    /**
     * ID of the external attendee.
     */
    id?: number | null;
    calendar: number;
};

/**
 * Write serializer for creating/rotating the org-level service account.
 *
 * ``private_key`` and ``private_key_id`` are write-only and are never echoed
 * back in any response (reads go through ``ServiceAccountReadSerializer``).
 */
export type ServiceAccountWriteWritable = {
    email: string;
    /**
     * Google Workspace super-admin email used as the DWD impersonation subject. The service account must have domain-wide delegation granted for the Admin SDK and Calendar API scopes in the Google Admin Console.
     */
    admin_email?: string;
    private_key_id: string;
    private_key: string;
};

export type UnavailableTimeWindowWritable = {
    id: number;
    reason: string;
    start_time: string;
    end_time: string;
};

export type WebhookConfigurationWritable = {
    event_type?: EventTypeEnum;
    url: string;
    headers?: unknown;
};

export type WebhookEventWritable = {
    event_type: EventTypeEnum;
    url: string;
    headers?: unknown;
    payload: unknown;
};

export type AvailableTimesListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * End time (less than or equal to)
         */
        end_time?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Start time (greater than or equal to)
         */
        start_time?: string;
    };
    url: '/available-times/';
};

export type AvailableTimesListResponses = {
    200: PaginatedAvailableTimeList;
};

export type AvailableTimesListResponse = AvailableTimesListResponses[keyof AvailableTimesListResponses];

export type AvailableTimesCreateData = {
    body: AvailableTimeWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/available-times/';
};

export type AvailableTimesCreateResponses = {
    201: AvailableTime;
};

export type AvailableTimesCreateResponse = AvailableTimesCreateResponses[keyof AvailableTimesCreateResponses];

export type AvailableTimesFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: {
        /**
         * End time (less than or equal to)
         */
        end_time?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Start time (greater than or equal to)
         */
        start_time?: string;
    };
    url: '/available-times{format}';
};

export type AvailableTimesFormattedListResponses = {
    200: PaginatedAvailableTimeList;
};

export type AvailableTimesFormattedListResponse = AvailableTimesFormattedListResponses[keyof AvailableTimesFormattedListResponses];

export type AvailableTimesFormattedCreateData = {
    body: AvailableTimeWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: never;
    url: '/available-times{format}';
};

export type AvailableTimesFormattedCreateResponses = {
    201: AvailableTime;
};

export type AvailableTimesFormattedCreateResponse = AvailableTimesFormattedCreateResponses[keyof AvailableTimesFormattedCreateResponses];

export type AvailableTimesDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/available-times/{id}/';
};

export type AvailableTimesDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type AvailableTimesDestroyResponse = AvailableTimesDestroyResponses[keyof AvailableTimesDestroyResponses];

export type AvailableTimesRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/available-times/{id}/';
};

export type AvailableTimesRetrieveResponses = {
    200: AvailableTime;
};

export type AvailableTimesRetrieveResponse = AvailableTimesRetrieveResponses[keyof AvailableTimesRetrieveResponses];

export type AvailableTimesPartialUpdateData = {
    body?: PatchedAvailableTimeWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/available-times/{id}/';
};

export type AvailableTimesPartialUpdateResponses = {
    200: AvailableTime;
};

export type AvailableTimesPartialUpdateResponse = AvailableTimesPartialUpdateResponses[keyof AvailableTimesPartialUpdateResponses];

export type AvailableTimesUpdateData = {
    body: AvailableTimeWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/available-times/{id}/';
};

export type AvailableTimesUpdateResponses = {
    200: AvailableTime;
};

export type AvailableTimesUpdateResponse = AvailableTimesUpdateResponses[keyof AvailableTimesUpdateResponses];

export type AvailableTimesFormattedDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/available-times/{id}{format}';
};

export type AvailableTimesFormattedDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type AvailableTimesFormattedDestroyResponse = AvailableTimesFormattedDestroyResponses[keyof AvailableTimesFormattedDestroyResponses];

export type AvailableTimesFormattedRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/available-times/{id}{format}';
};

export type AvailableTimesFormattedRetrieveResponses = {
    200: AvailableTime;
};

export type AvailableTimesFormattedRetrieveResponse = AvailableTimesFormattedRetrieveResponses[keyof AvailableTimesFormattedRetrieveResponses];

export type AvailableTimesFormattedPartialUpdateData = {
    body?: PatchedAvailableTimeWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/available-times/{id}{format}';
};

export type AvailableTimesFormattedPartialUpdateResponses = {
    200: AvailableTime;
};

export type AvailableTimesFormattedPartialUpdateResponse = AvailableTimesFormattedPartialUpdateResponses[keyof AvailableTimesFormattedPartialUpdateResponses];

export type AvailableTimesFormattedUpdateData = {
    body: AvailableTimeWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/available-times/{id}{format}';
};

export type AvailableTimesFormattedUpdateResponses = {
    200: AvailableTime;
};

export type AvailableTimesFormattedUpdateResponse = AvailableTimesFormattedUpdateResponses[keyof AvailableTimesFormattedUpdateResponses];

export type AvailableTimesBulkModifyCreateData = {
    body: AvailableTimeBulkModificationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/available-times/{id}/bulk-modify/';
};

export type AvailableTimesBulkModifyCreateResponses = {
    200: AvailableTime;
    /**
     * No response body
     */
    204: void;
};

export type AvailableTimesBulkModifyCreateResponse = AvailableTimesBulkModifyCreateResponses[keyof AvailableTimesBulkModifyCreateResponses];

export type AvailableTimesBulkModifyFormattedCreateData = {
    body: AvailableTimeBulkModificationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/available-times/{id}/bulk-modify{format}';
};

export type AvailableTimesBulkModifyFormattedCreateResponses = {
    200: AvailableTime;
    /**
     * No response body
     */
    204: void;
};

export type AvailableTimesBulkModifyFormattedCreateResponse = AvailableTimesBulkModifyFormattedCreateResponses[keyof AvailableTimesBulkModifyFormattedCreateResponses];

export type AvailableTimesCreateExceptionCreateData = {
    body: AvailableTimeRecurringException;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/available-times/{id}/create-exception/';
};

export type AvailableTimesCreateExceptionCreateResponses = {
    201: AvailableTime;
    /**
     * No response body
     */
    204: void;
};

export type AvailableTimesCreateExceptionCreateResponse = AvailableTimesCreateExceptionCreateResponses[keyof AvailableTimesCreateExceptionCreateResponses];

export type AvailableTimesCreateExceptionFormattedCreateData = {
    body: AvailableTimeRecurringException;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/available-times/{id}/create-exception{format}';
};

export type AvailableTimesCreateExceptionFormattedCreateResponses = {
    201: AvailableTime;
    /**
     * No response body
     */
    204: void;
};

export type AvailableTimesCreateExceptionFormattedCreateResponse = AvailableTimesCreateExceptionFormattedCreateResponses[keyof AvailableTimesCreateExceptionFormattedCreateResponses];

export type AvailableTimesBatchCreateData = {
    body: AvailableTimeBatch;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * End time (less than or equal to)
         */
        end_time?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Start time (greater than or equal to)
         */
        start_time?: string;
    };
    url: '/available-times/batch/';
};

export type AvailableTimesBatchCreateResponses = {
    200: PaginatedAvailableTimeList;
};

export type AvailableTimesBatchCreateResponse = AvailableTimesBatchCreateResponses[keyof AvailableTimesBatchCreateResponses];

export type AvailableTimesBatchFormattedCreateData = {
    body: AvailableTimeBatch;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: {
        /**
         * End time (less than or equal to)
         */
        end_time?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Start time (greater than or equal to)
         */
        start_time?: string;
    };
    url: '/available-times/batch{format}';
};

export type AvailableTimesBatchFormattedCreateResponses = {
    200: PaginatedAvailableTimeList;
};

export type AvailableTimesBatchFormattedCreateResponse = AvailableTimesBatchFormattedCreateResponses[keyof AvailableTimesBatchFormattedCreateResponses];

export type AvailableTimesExpandedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query: {
        /**
         * Calendar ID to get available times for
         */
        calendar_id: number;
        /**
         * End datetime for the range (ISO format)
         */
        end_datetime: string;
        /**
         * End time (less than or equal to)
         */
        end_time?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Start datetime for the range (ISO format)
         */
        start_datetime: string;
        /**
         * Start time (greater than or equal to)
         */
        start_time?: string;
    };
    url: '/available-times/expanded/';
};

export type AvailableTimesExpandedListResponses = {
    200: PaginatedAvailableTimeList;
};

export type AvailableTimesExpandedListResponse = AvailableTimesExpandedListResponses[keyof AvailableTimesExpandedListResponses];

export type AvailableTimesExpandedFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query: {
        /**
         * Calendar ID to get available times for
         */
        calendar_id: number;
        /**
         * End datetime for the range (ISO format)
         */
        end_datetime: string;
        /**
         * End time (less than or equal to)
         */
        end_time?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Start datetime for the range (ISO format)
         */
        start_datetime: string;
        /**
         * Start time (greater than or equal to)
         */
        start_time?: string;
    };
    url: '/available-times/expanded{format}';
};

export type AvailableTimesExpandedFormattedListResponses = {
    200: PaginatedAvailableTimeList;
};

export type AvailableTimesExpandedFormattedListResponse = AvailableTimesExpandedFormattedListResponses[keyof AvailableTimesExpandedFormattedListResponses];

export type BillingProfileCreateBillingProfileCreateData = {
    body: BillingProfileWritable;
    path?: never;
    query?: never;
    url: '/billing-profile/create_billing_profile/';
};

export type BillingProfileCreateBillingProfileCreateResponses = {
    201: BillingProfile;
};

export type BillingProfileCreateBillingProfileCreateResponse = BillingProfileCreateBillingProfileCreateResponses[keyof BillingProfileCreateBillingProfileCreateResponses];

export type BillingProfileCreateBillingProfileFormattedCreateData = {
    body: BillingProfileWritable;
    path: {
        format: '.json';
    };
    query?: never;
    url: '/billing-profile/create_billing_profile{format}';
};

export type BillingProfileCreateBillingProfileFormattedCreateResponses = {
    201: BillingProfile;
};

export type BillingProfileCreateBillingProfileFormattedCreateResponse = BillingProfileCreateBillingProfileFormattedCreateResponses[keyof BillingProfileCreateBillingProfileFormattedCreateResponses];

export type BillingProfilePartialUpdateBillingProfilePartialUpdateData = {
    body?: PatchedBillingProfileWritable;
    path?: never;
    query?: never;
    url: '/billing-profile/partial_update_billing_profile/';
};

export type BillingProfilePartialUpdateBillingProfilePartialUpdateResponses = {
    200: BillingProfile;
};

export type BillingProfilePartialUpdateBillingProfilePartialUpdateResponse = BillingProfilePartialUpdateBillingProfilePartialUpdateResponses[keyof BillingProfilePartialUpdateBillingProfilePartialUpdateResponses];

export type BillingProfilePartialUpdateBillingProfileFormattedPartialUpdateData = {
    body?: PatchedBillingProfileWritable;
    path: {
        format: '.json';
    };
    query?: never;
    url: '/billing-profile/partial_update_billing_profile{format}';
};

export type BillingProfilePartialUpdateBillingProfileFormattedPartialUpdateResponses = {
    200: BillingProfile;
};

export type BillingProfilePartialUpdateBillingProfileFormattedPartialUpdateResponse = BillingProfilePartialUpdateBillingProfileFormattedPartialUpdateResponses[keyof BillingProfilePartialUpdateBillingProfileFormattedPartialUpdateResponses];

export type BillingProfileRetrieveBillingProfileRetrieveData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/billing-profile/retrieve_billing_profile/';
};

export type BillingProfileRetrieveBillingProfileRetrieveResponses = {
    200: BillingProfile;
};

export type BillingProfileRetrieveBillingProfileRetrieveResponse = BillingProfileRetrieveBillingProfileRetrieveResponses[keyof BillingProfileRetrieveBillingProfileRetrieveResponses];

export type BillingProfileRetrieveBillingProfileFormattedRetrieveData = {
    body?: never;
    path: {
        format: '.json';
    };
    query?: never;
    url: '/billing-profile/retrieve_billing_profile{format}';
};

export type BillingProfileRetrieveBillingProfileFormattedRetrieveResponses = {
    200: BillingProfile;
};

export type BillingProfileRetrieveBillingProfileFormattedRetrieveResponse = BillingProfileRetrieveBillingProfileFormattedRetrieveResponses[keyof BillingProfileRetrieveBillingProfileFormattedRetrieveResponses];

export type BillingProfileUpdateBillingProfileUpdateData = {
    body: BillingProfileWritable;
    path?: never;
    query?: never;
    url: '/billing-profile/update_billing_profile/';
};

export type BillingProfileUpdateBillingProfileUpdateResponses = {
    200: BillingProfile;
};

export type BillingProfileUpdateBillingProfileUpdateResponse = BillingProfileUpdateBillingProfileUpdateResponses[keyof BillingProfileUpdateBillingProfileUpdateResponses];

export type BillingProfileUpdateBillingProfileFormattedUpdateData = {
    body: BillingProfileWritable;
    path: {
        format: '.json';
    };
    query?: never;
    url: '/billing-profile/update_billing_profile{format}';
};

export type BillingProfileUpdateBillingProfileFormattedUpdateResponses = {
    200: BillingProfile;
};

export type BillingProfileUpdateBillingProfileFormattedUpdateResponse = BillingProfileUpdateBillingProfileFormattedUpdateResponses[keyof BillingProfileUpdateBillingProfileFormattedUpdateResponses];

export type BlockedTimesListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * End time (less than or equal to)
         */
        end_time?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Filter by partial title match
         */
        reason?: string;
        /**
         * Start time (greater than or equal to)
         */
        start_time?: string;
    };
    url: '/blocked-times/';
};

export type BlockedTimesListResponses = {
    200: PaginatedBlockedTimeList;
};

export type BlockedTimesListResponse = BlockedTimesListResponses[keyof BlockedTimesListResponses];

export type BlockedTimesCreateData = {
    body: BlockedTimeWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/blocked-times/';
};

export type BlockedTimesCreateResponses = {
    201: BlockedTime;
};

export type BlockedTimesCreateResponse = BlockedTimesCreateResponses[keyof BlockedTimesCreateResponses];

export type BlockedTimesFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: {
        /**
         * End time (less than or equal to)
         */
        end_time?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Filter by partial title match
         */
        reason?: string;
        /**
         * Start time (greater than or equal to)
         */
        start_time?: string;
    };
    url: '/blocked-times{format}';
};

export type BlockedTimesFormattedListResponses = {
    200: PaginatedBlockedTimeList;
};

export type BlockedTimesFormattedListResponse = BlockedTimesFormattedListResponses[keyof BlockedTimesFormattedListResponses];

export type BlockedTimesFormattedCreateData = {
    body: BlockedTimeWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: never;
    url: '/blocked-times{format}';
};

export type BlockedTimesFormattedCreateResponses = {
    201: BlockedTime;
};

export type BlockedTimesFormattedCreateResponse = BlockedTimesFormattedCreateResponses[keyof BlockedTimesFormattedCreateResponses];

export type BlockedTimesDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/blocked-times/{id}/';
};

export type BlockedTimesDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type BlockedTimesDestroyResponse = BlockedTimesDestroyResponses[keyof BlockedTimesDestroyResponses];

export type BlockedTimesRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/blocked-times/{id}/';
};

export type BlockedTimesRetrieveResponses = {
    200: BlockedTime;
};

export type BlockedTimesRetrieveResponse = BlockedTimesRetrieveResponses[keyof BlockedTimesRetrieveResponses];

export type BlockedTimesPartialUpdateData = {
    body?: PatchedBlockedTimeWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/blocked-times/{id}/';
};

export type BlockedTimesPartialUpdateResponses = {
    200: BlockedTime;
};

export type BlockedTimesPartialUpdateResponse = BlockedTimesPartialUpdateResponses[keyof BlockedTimesPartialUpdateResponses];

export type BlockedTimesUpdateData = {
    body: BlockedTimeWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/blocked-times/{id}/';
};

export type BlockedTimesUpdateResponses = {
    200: BlockedTime;
};

export type BlockedTimesUpdateResponse = BlockedTimesUpdateResponses[keyof BlockedTimesUpdateResponses];

export type BlockedTimesFormattedDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/blocked-times/{id}{format}';
};

export type BlockedTimesFormattedDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type BlockedTimesFormattedDestroyResponse = BlockedTimesFormattedDestroyResponses[keyof BlockedTimesFormattedDestroyResponses];

export type BlockedTimesFormattedRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/blocked-times/{id}{format}';
};

export type BlockedTimesFormattedRetrieveResponses = {
    200: BlockedTime;
};

export type BlockedTimesFormattedRetrieveResponse = BlockedTimesFormattedRetrieveResponses[keyof BlockedTimesFormattedRetrieveResponses];

export type BlockedTimesFormattedPartialUpdateData = {
    body?: PatchedBlockedTimeWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/blocked-times/{id}{format}';
};

export type BlockedTimesFormattedPartialUpdateResponses = {
    200: BlockedTime;
};

export type BlockedTimesFormattedPartialUpdateResponse = BlockedTimesFormattedPartialUpdateResponses[keyof BlockedTimesFormattedPartialUpdateResponses];

export type BlockedTimesFormattedUpdateData = {
    body: BlockedTimeWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/blocked-times/{id}{format}';
};

export type BlockedTimesFormattedUpdateResponses = {
    200: BlockedTime;
};

export type BlockedTimesFormattedUpdateResponse = BlockedTimesFormattedUpdateResponses[keyof BlockedTimesFormattedUpdateResponses];

export type BlockedTimesBulkModifyCreateData = {
    body: BlockedTimeBulkModificationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/blocked-times/{id}/bulk-modify/';
};

export type BlockedTimesBulkModifyCreateResponses = {
    200: BlockedTime;
    /**
     * No response body
     */
    204: void;
};

export type BlockedTimesBulkModifyCreateResponse = BlockedTimesBulkModifyCreateResponses[keyof BlockedTimesBulkModifyCreateResponses];

export type BlockedTimesBulkModifyFormattedCreateData = {
    body: BlockedTimeBulkModificationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/blocked-times/{id}/bulk-modify{format}';
};

export type BlockedTimesBulkModifyFormattedCreateResponses = {
    200: BlockedTime;
    /**
     * No response body
     */
    204: void;
};

export type BlockedTimesBulkModifyFormattedCreateResponse = BlockedTimesBulkModifyFormattedCreateResponses[keyof BlockedTimesBulkModifyFormattedCreateResponses];

export type BlockedTimesCreateExceptionCreateData = {
    body: BlockedTimeRecurringException;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/blocked-times/{id}/create-exception/';
};

export type BlockedTimesCreateExceptionCreateResponses = {
    201: BlockedTime;
    /**
     * No response body
     */
    204: void;
};

export type BlockedTimesCreateExceptionCreateResponse = BlockedTimesCreateExceptionCreateResponses[keyof BlockedTimesCreateExceptionCreateResponses];

export type BlockedTimesCreateExceptionFormattedCreateData = {
    body: BlockedTimeRecurringException;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/blocked-times/{id}/create-exception{format}';
};

export type BlockedTimesCreateExceptionFormattedCreateResponses = {
    201: BlockedTime;
    /**
     * No response body
     */
    204: void;
};

export type BlockedTimesCreateExceptionFormattedCreateResponse = BlockedTimesCreateExceptionFormattedCreateResponses[keyof BlockedTimesCreateExceptionFormattedCreateResponses];

export type BlockedTimesBulkCreateCreateData = {
    body: BulkBlockedTimeWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * End time (less than or equal to)
         */
        end_time?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Filter by partial title match
         */
        reason?: string;
        /**
         * Start time (greater than or equal to)
         */
        start_time?: string;
    };
    url: '/blocked-times/bulk-create/';
};

export type BlockedTimesBulkCreateCreateResponses = {
    201: PaginatedBlockedTimeList;
};

export type BlockedTimesBulkCreateCreateResponse = BlockedTimesBulkCreateCreateResponses[keyof BlockedTimesBulkCreateCreateResponses];

export type BlockedTimesBulkCreateFormattedCreateData = {
    body: BulkBlockedTimeWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: {
        /**
         * End time (less than or equal to)
         */
        end_time?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Filter by partial title match
         */
        reason?: string;
        /**
         * Start time (greater than or equal to)
         */
        start_time?: string;
    };
    url: '/blocked-times/bulk-create{format}';
};

export type BlockedTimesBulkCreateFormattedCreateResponses = {
    201: PaginatedBlockedTimeList;
};

export type BlockedTimesBulkCreateFormattedCreateResponse = BlockedTimesBulkCreateFormattedCreateResponses[keyof BlockedTimesBulkCreateFormattedCreateResponses];

export type BlockedTimesExpandedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query: {
        /**
         * Calendar ID to get blocked times for
         */
        calendar_id: number;
        /**
         * End datetime for the range (ISO format)
         */
        end_datetime: string;
        /**
         * End time (less than or equal to)
         */
        end_time?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Filter by partial title match
         */
        reason?: string;
        /**
         * Start datetime for the range (ISO format)
         */
        start_datetime: string;
        /**
         * Start time (greater than or equal to)
         */
        start_time?: string;
    };
    url: '/blocked-times/expanded/';
};

export type BlockedTimesExpandedListResponses = {
    200: PaginatedBlockedTimeList;
};

export type BlockedTimesExpandedListResponse = BlockedTimesExpandedListResponses[keyof BlockedTimesExpandedListResponses];

export type BlockedTimesExpandedFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query: {
        /**
         * Calendar ID to get blocked times for
         */
        calendar_id: number;
        /**
         * End datetime for the range (ISO format)
         */
        end_datetime: string;
        /**
         * End time (less than or equal to)
         */
        end_time?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Filter by partial title match
         */
        reason?: string;
        /**
         * Start datetime for the range (ISO format)
         */
        start_datetime: string;
        /**
         * Start time (greater than or equal to)
         */
        start_time?: string;
    };
    url: '/blocked-times/expanded{format}';
};

export type BlockedTimesExpandedFormattedListResponses = {
    200: PaginatedBlockedTimeList;
};

export type BlockedTimesExpandedFormattedListResponse = BlockedTimesExpandedFormattedListResponses[keyof BlockedTimesExpandedFormattedListResponses];

export type BrandingRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/branding/';
};

export type BrandingRetrieveErrors = {
    /**
     * Not a reseller or not an admin
     */
    403: unknown;
    /**
     * Branding not yet configured
     */
    404: unknown;
};

export type BrandingRetrieveResponses = {
    200: OrganizationBranding;
};

export type BrandingRetrieveResponse = BrandingRetrieveResponses[keyof BrandingRetrieveResponses];

export type BrandingPartialUpdateData = {
    body?: PatchedOrganizationBranding;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/branding/';
};

export type BrandingPartialUpdateErrors = {
    /**
     * Invalid input (color format, URL validation)
     */
    400: unknown;
    /**
     * Not a reseller or not an admin
     */
    403: unknown;
    /**
     * Branding not yet configured
     */
    404: unknown;
};

export type BrandingPartialUpdateResponses = {
    200: OrganizationBranding;
};

export type BrandingPartialUpdateResponse = BrandingPartialUpdateResponses[keyof BrandingPartialUpdateResponses];

export type BrandingUpdateData = {
    body: OrganizationBranding;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/branding/';
};

export type BrandingUpdateErrors = {
    /**
     * Invalid input (color format, URL validation)
     */
    400: unknown;
    /**
     * Not a reseller or not an admin
     */
    403: unknown;
};

export type BrandingUpdateResponses = {
    200: OrganizationBranding;
    201: OrganizationBranding;
};

export type BrandingUpdateResponse = BrandingUpdateResponses[keyof BrandingUpdateResponses];

export type CalendarListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Filter by calendar type (e.g. resource)
         *
         * * `personal` - Personal Calendar
         * * `resource` - Resource Calendar
         * * `virtual` - Virtual Calendar
         * * `bundle` - Bundle Calendar
         */
        calendar_type?: 'bundle' | 'personal' | 'resource' | 'virtual';
        /**
         * When true, include inactive (soft-deleted) calendars (visibility=inactive). Defaults to false.
         */
        include_inactive?: boolean;
        /**
         * When true, include unlisted calendars (visibility=unlisted) in the response. Unlisted calendars are hidden from booking queries but still synced. Defaults to false.
         */
        include_unlisted?: boolean;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Scope the listing to a calendar owner. Pass 'me' to return only the authenticated user's own calendars. Pass a numeric user id to return that user's calendars — allowed for organization admins only; non-admins receive 403. When omitted, admins see all organization calendars while non-admins are restricted to their own.
         */
        owner?: string;
        /**
         * Filter by provider (internal = manual, others = synced)
         *
         * * `internal` - Internal Calendar
         * * `google` - Google Calendar
         * * `microsoft` - Microsoft Outlook Calendar
         * * `apple` - Apple Calendar
         * * `ics` - ICS
         */
        provider?: 'apple' | 'google' | 'ics' | 'internal' | 'microsoft';
        /**
         * Filter by whether provider sync is enabled
         */
        sync_enabled?: boolean;
    };
    url: '/calendar/';
};

export type CalendarListResponses = {
    200: PaginatedCalendarList;
};

export type CalendarListResponse = CalendarListResponses[keyof CalendarListResponses];

export type CalendarCreateData = {
    body: CalendarWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/calendar/';
};

export type CalendarCreateResponses = {
    201: Calendar;
};

export type CalendarCreateResponse = CalendarCreateResponses[keyof CalendarCreateResponses];

export type CalendarFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: {
        /**
         * Filter by calendar type (e.g. resource)
         *
         * * `personal` - Personal Calendar
         * * `resource` - Resource Calendar
         * * `virtual` - Virtual Calendar
         * * `bundle` - Bundle Calendar
         */
        calendar_type?: 'bundle' | 'personal' | 'resource' | 'virtual';
        /**
         * When true, include inactive (soft-deleted) calendars (visibility=inactive). Defaults to false.
         */
        include_inactive?: boolean;
        /**
         * When true, include unlisted calendars (visibility=unlisted) in the response. Unlisted calendars are hidden from booking queries but still synced. Defaults to false.
         */
        include_unlisted?: boolean;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Scope the listing to a calendar owner. Pass 'me' to return only the authenticated user's own calendars. Pass a numeric user id to return that user's calendars — allowed for organization admins only; non-admins receive 403. When omitted, admins see all organization calendars while non-admins are restricted to their own.
         */
        owner?: string;
        /**
         * Filter by provider (internal = manual, others = synced)
         *
         * * `internal` - Internal Calendar
         * * `google` - Google Calendar
         * * `microsoft` - Microsoft Outlook Calendar
         * * `apple` - Apple Calendar
         * * `ics` - ICS
         */
        provider?: 'apple' | 'google' | 'ics' | 'internal' | 'microsoft';
        /**
         * Filter by whether provider sync is enabled
         */
        sync_enabled?: boolean;
    };
    url: '/calendar{format}';
};

export type CalendarFormattedListResponses = {
    200: PaginatedCalendarList;
};

export type CalendarFormattedListResponse = CalendarFormattedListResponses[keyof CalendarFormattedListResponses];

export type CalendarFormattedCreateData = {
    body: CalendarWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: never;
    url: '/calendar{format}';
};

export type CalendarFormattedCreateResponses = {
    201: Calendar;
};

export type CalendarFormattedCreateResponse = CalendarFormattedCreateResponses[keyof CalendarFormattedCreateResponses];

export type CalendarEventsListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Filter by calendar ID
         */
        calendar?: number;
        /**
         * End time (less than or equal to)
         */
        end_time?: string;
        /**
         * End time range
         */
        end_time_range_after?: string;
        /**
         * End time range
         */
        end_time_range_before?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Start time (greater than or equal to)
         */
        start_time?: string;
        /**
         * Start time range
         */
        start_time_range_after?: string;
        /**
         * Start time range
         */
        start_time_range_before?: string;
        /**
         * Filter by partial title match
         */
        title?: string;
    };
    url: '/calendar-events/';
};

export type CalendarEventsListResponses = {
    200: PaginatedCalendarEventList;
};

export type CalendarEventsListResponse = CalendarEventsListResponses[keyof CalendarEventsListResponses];

export type CalendarEventsCreateData = {
    body: CalendarEventWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/calendar-events/';
};

export type CalendarEventsCreateResponses = {
    201: CalendarEvent;
};

export type CalendarEventsCreateResponse = CalendarEventsCreateResponses[keyof CalendarEventsCreateResponses];

export type CalendarEventsFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: {
        /**
         * Filter by calendar ID
         */
        calendar?: number;
        /**
         * End time (less than or equal to)
         */
        end_time?: string;
        /**
         * End time range
         */
        end_time_range_after?: string;
        /**
         * End time range
         */
        end_time_range_before?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Start time (greater than or equal to)
         */
        start_time?: string;
        /**
         * Start time range
         */
        start_time_range_after?: string;
        /**
         * Start time range
         */
        start_time_range_before?: string;
        /**
         * Filter by partial title match
         */
        title?: string;
    };
    url: '/calendar-events{format}';
};

export type CalendarEventsFormattedListResponses = {
    200: PaginatedCalendarEventList;
};

export type CalendarEventsFormattedListResponse = CalendarEventsFormattedListResponses[keyof CalendarEventsFormattedListResponses];

export type CalendarEventsFormattedCreateData = {
    body: CalendarEventWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: never;
    url: '/calendar-events{format}';
};

export type CalendarEventsFormattedCreateResponses = {
    201: CalendarEvent;
};

export type CalendarEventsFormattedCreateResponse = CalendarEventsFormattedCreateResponses[keyof CalendarEventsFormattedCreateResponses];

export type CalendarEventsDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar-events/{id}/';
};

export type CalendarEventsDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type CalendarEventsDestroyResponse = CalendarEventsDestroyResponses[keyof CalendarEventsDestroyResponses];

export type CalendarEventsRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar-events/{id}/';
};

export type CalendarEventsRetrieveResponses = {
    200: CalendarEvent;
};

export type CalendarEventsRetrieveResponse = CalendarEventsRetrieveResponses[keyof CalendarEventsRetrieveResponses];

export type CalendarEventsPartialUpdateData = {
    body?: PatchedCalendarEventWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar-events/{id}/';
};

export type CalendarEventsPartialUpdateResponses = {
    200: CalendarEvent;
};

export type CalendarEventsPartialUpdateResponse = CalendarEventsPartialUpdateResponses[keyof CalendarEventsPartialUpdateResponses];

export type CalendarEventsUpdateData = {
    body: CalendarEventWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar-events/{id}/';
};

export type CalendarEventsUpdateResponses = {
    200: CalendarEvent;
};

export type CalendarEventsUpdateResponse = CalendarEventsUpdateResponses[keyof CalendarEventsUpdateResponses];

export type CalendarEventsFormattedDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar-events/{id}{format}';
};

export type CalendarEventsFormattedDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type CalendarEventsFormattedDestroyResponse = CalendarEventsFormattedDestroyResponses[keyof CalendarEventsFormattedDestroyResponses];

export type CalendarEventsFormattedRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar-events/{id}{format}';
};

export type CalendarEventsFormattedRetrieveResponses = {
    200: CalendarEvent;
};

export type CalendarEventsFormattedRetrieveResponse = CalendarEventsFormattedRetrieveResponses[keyof CalendarEventsFormattedRetrieveResponses];

export type CalendarEventsFormattedPartialUpdateData = {
    body?: PatchedCalendarEventWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar-events/{id}{format}';
};

export type CalendarEventsFormattedPartialUpdateResponses = {
    200: CalendarEvent;
};

export type CalendarEventsFormattedPartialUpdateResponse = CalendarEventsFormattedPartialUpdateResponses[keyof CalendarEventsFormattedPartialUpdateResponses];

export type CalendarEventsFormattedUpdateData = {
    body: CalendarEventWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar-events/{id}{format}';
};

export type CalendarEventsFormattedUpdateResponses = {
    200: CalendarEvent;
};

export type CalendarEventsFormattedUpdateResponse = CalendarEventsFormattedUpdateResponses[keyof CalendarEventsFormattedUpdateResponses];

export type CalendarEventsBulkModifyCreateData = {
    body: EventBulkModificationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar-events/{id}/bulk-modify/';
};

export type CalendarEventsBulkModifyCreateResponses = {
    200: CalendarEvent;
    /**
     * No response body
     */
    204: void;
};

export type CalendarEventsBulkModifyCreateResponse = CalendarEventsBulkModifyCreateResponses[keyof CalendarEventsBulkModifyCreateResponses];

export type CalendarEventsBulkModifyFormattedCreateData = {
    body: EventBulkModificationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar-events/{id}/bulk-modify{format}';
};

export type CalendarEventsBulkModifyFormattedCreateResponses = {
    200: CalendarEvent;
    /**
     * No response body
     */
    204: void;
};

export type CalendarEventsBulkModifyFormattedCreateResponse = CalendarEventsBulkModifyFormattedCreateResponses[keyof CalendarEventsBulkModifyFormattedCreateResponses];

export type CalendarEventsCreateExceptionCreateData = {
    body: EventRecurringException;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar-events/{id}/create-exception/';
};

export type CalendarEventsCreateExceptionCreateResponses = {
    201: CalendarEvent;
    /**
     * No response body
     */
    204: void;
};

export type CalendarEventsCreateExceptionCreateResponse = CalendarEventsCreateExceptionCreateResponses[keyof CalendarEventsCreateExceptionCreateResponses];

export type CalendarEventsCreateExceptionFormattedCreateData = {
    body: EventRecurringException;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar-events/{id}/create-exception{format}';
};

export type CalendarEventsCreateExceptionFormattedCreateResponses = {
    201: CalendarEvent;
    /**
     * No response body
     */
    204: void;
};

export type CalendarEventsCreateExceptionFormattedCreateResponse = CalendarEventsCreateExceptionFormattedCreateResponses[keyof CalendarEventsCreateExceptionFormattedCreateResponses];

export type CalendarEventsTransferCreateData = {
    body: CalendarEventTransfer;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar-events/{id}/transfer/';
};

export type CalendarEventsTransferCreateResponses = {
    200: CalendarEvent;
};

export type CalendarEventsTransferCreateResponse = CalendarEventsTransferCreateResponses[keyof CalendarEventsTransferCreateResponses];

export type CalendarEventsTransferFormattedCreateData = {
    body: CalendarEventTransfer;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar-events/{id}/transfer{format}';
};

export type CalendarEventsTransferFormattedCreateResponses = {
    200: CalendarEvent;
};

export type CalendarEventsTransferFormattedCreateResponse = CalendarEventsTransferFormattedCreateResponses[keyof CalendarEventsTransferFormattedCreateResponses];

export type CalendarEventsExpandedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query: {
        /**
         * Filter by calendar ID
         */
        calendar?: number;
        /**
         * Calendar ID to get events for
         */
        calendar_id: number;
        /**
         * End datetime for the range (ISO format)
         */
        end_time: string;
        /**
         * End time range
         */
        end_time_range_after?: string;
        /**
         * End time range
         */
        end_time_range_before?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Start datetime for the range (ISO format)
         */
        start_time: string;
        /**
         * Start time range
         */
        start_time_range_after?: string;
        /**
         * Start time range
         */
        start_time_range_before?: string;
        /**
         * Filter by partial title match
         */
        title?: string;
    };
    url: '/calendar-events/expanded/';
};

export type CalendarEventsExpandedListResponses = {
    200: PaginatedCalendarEventList;
};

export type CalendarEventsExpandedListResponse = CalendarEventsExpandedListResponses[keyof CalendarEventsExpandedListResponses];

export type CalendarEventsExpandedFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query: {
        /**
         * Filter by calendar ID
         */
        calendar?: number;
        /**
         * Calendar ID to get events for
         */
        calendar_id: number;
        /**
         * End datetime for the range (ISO format)
         */
        end_time: string;
        /**
         * End time range
         */
        end_time_range_after?: string;
        /**
         * End time range
         */
        end_time_range_before?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Start datetime for the range (ISO format)
         */
        start_time: string;
        /**
         * Start time range
         */
        start_time_range_after?: string;
        /**
         * Start time range
         */
        start_time_range_before?: string;
        /**
         * Filter by partial title match
         */
        title?: string;
    };
    url: '/calendar-events/expanded{format}';
};

export type CalendarEventsExpandedFormattedListResponses = {
    200: PaginatedCalendarEventList;
};

export type CalendarEventsExpandedFormattedListResponse = CalendarEventsExpandedFormattedListResponses[keyof CalendarEventsExpandedFormattedListResponses];

export type CalendarGroupsListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * Filter by partial name match
         */
        name?: string;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/calendar-groups/';
};

export type CalendarGroupsListResponses = {
    200: PaginatedCalendarGroupList;
};

export type CalendarGroupsListResponse = CalendarGroupsListResponses[keyof CalendarGroupsListResponses];

export type CalendarGroupsCreateData = {
    body: CalendarGroupWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/calendar-groups/';
};

export type CalendarGroupsCreateResponses = {
    201: CalendarGroup;
};

export type CalendarGroupsCreateResponse = CalendarGroupsCreateResponses[keyof CalendarGroupsCreateResponses];

export type CalendarGroupsFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: {
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * Filter by partial name match
         */
        name?: string;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/calendar-groups{format}';
};

export type CalendarGroupsFormattedListResponses = {
    200: PaginatedCalendarGroupList;
};

export type CalendarGroupsFormattedListResponse = CalendarGroupsFormattedListResponses[keyof CalendarGroupsFormattedListResponses];

export type CalendarGroupsFormattedCreateData = {
    body: CalendarGroupWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: never;
    url: '/calendar-groups{format}';
};

export type CalendarGroupsFormattedCreateResponses = {
    201: CalendarGroup;
};

export type CalendarGroupsFormattedCreateResponse = CalendarGroupsFormattedCreateResponses[keyof CalendarGroupsFormattedCreateResponses];

export type CalendarGroupsDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar-groups/{id}/';
};

export type CalendarGroupsDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type CalendarGroupsDestroyResponse = CalendarGroupsDestroyResponses[keyof CalendarGroupsDestroyResponses];

export type CalendarGroupsRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar-groups/{id}/';
};

export type CalendarGroupsRetrieveResponses = {
    200: CalendarGroup;
};

export type CalendarGroupsRetrieveResponse = CalendarGroupsRetrieveResponses[keyof CalendarGroupsRetrieveResponses];

export type CalendarGroupsPartialUpdateData = {
    body?: PatchedCalendarGroupWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar-groups/{id}/';
};

export type CalendarGroupsPartialUpdateResponses = {
    200: CalendarGroup;
};

export type CalendarGroupsPartialUpdateResponse = CalendarGroupsPartialUpdateResponses[keyof CalendarGroupsPartialUpdateResponses];

export type CalendarGroupsUpdateData = {
    body: CalendarGroupWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar-groups/{id}/';
};

export type CalendarGroupsUpdateResponses = {
    200: CalendarGroup;
};

export type CalendarGroupsUpdateResponse = CalendarGroupsUpdateResponses[keyof CalendarGroupsUpdateResponses];

export type CalendarGroupsFormattedDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar-groups/{id}{format}';
};

export type CalendarGroupsFormattedDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type CalendarGroupsFormattedDestroyResponse = CalendarGroupsFormattedDestroyResponses[keyof CalendarGroupsFormattedDestroyResponses];

export type CalendarGroupsFormattedRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar-groups/{id}{format}';
};

export type CalendarGroupsFormattedRetrieveResponses = {
    200: CalendarGroup;
};

export type CalendarGroupsFormattedRetrieveResponse = CalendarGroupsFormattedRetrieveResponses[keyof CalendarGroupsFormattedRetrieveResponses];

export type CalendarGroupsFormattedPartialUpdateData = {
    body?: PatchedCalendarGroupWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar-groups/{id}{format}';
};

export type CalendarGroupsFormattedPartialUpdateResponses = {
    200: CalendarGroup;
};

export type CalendarGroupsFormattedPartialUpdateResponse = CalendarGroupsFormattedPartialUpdateResponses[keyof CalendarGroupsFormattedPartialUpdateResponses];

export type CalendarGroupsFormattedUpdateData = {
    body: CalendarGroupWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar-groups/{id}{format}';
};

export type CalendarGroupsFormattedUpdateResponses = {
    200: CalendarGroup;
};

export type CalendarGroupsFormattedUpdateResponse = CalendarGroupsFormattedUpdateResponses[keyof CalendarGroupsFormattedUpdateResponses];

export type CalendarGroupsAvailabilityCreateData = {
    body: CalendarGroupAvailabilityQuery;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: {
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * Filter by partial name match
         */
        name?: string;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/calendar-groups/{id}/availability/';
};

export type CalendarGroupsAvailabilityCreateResponses = {
    200: PaginatedCalendarGroupRangeAvailabilityList;
};

export type CalendarGroupsAvailabilityCreateResponse = CalendarGroupsAvailabilityCreateResponses[keyof CalendarGroupsAvailabilityCreateResponses];

export type CalendarGroupsAvailabilityFormattedCreateData = {
    body: CalendarGroupAvailabilityQuery;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: {
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * Filter by partial name match
         */
        name?: string;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/calendar-groups/{id}/availability{format}';
};

export type CalendarGroupsAvailabilityFormattedCreateResponses = {
    200: PaginatedCalendarGroupRangeAvailabilityList;
};

export type CalendarGroupsAvailabilityFormattedCreateResponse = CalendarGroupsAvailabilityFormattedCreateResponses[keyof CalendarGroupsAvailabilityFormattedCreateResponses];

export type CalendarGroupsBookableSlotsListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query: {
        /**
         * Desired event duration, in seconds
         */
        duration_seconds: number;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * Filter by partial name match
         */
        name?: string;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * End of the search window (ISO 8601)
         */
        search_window_end: string;
        /**
         * Start of the search window (ISO 8601)
         */
        search_window_start: string;
        /**
         * Search step, in seconds (default 900 = 15min)
         */
        slot_step_seconds?: number;
    };
    url: '/calendar-groups/{id}/bookable-slots/';
};

export type CalendarGroupsBookableSlotsListResponses = {
    200: PaginatedBookableSlotProposalList;
};

export type CalendarGroupsBookableSlotsListResponse = CalendarGroupsBookableSlotsListResponses[keyof CalendarGroupsBookableSlotsListResponses];

export type CalendarGroupsBookableSlotsFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query: {
        /**
         * Desired event duration, in seconds
         */
        duration_seconds: number;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * Filter by partial name match
         */
        name?: string;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * End of the search window (ISO 8601)
         */
        search_window_end: string;
        /**
         * Start of the search window (ISO 8601)
         */
        search_window_start: string;
        /**
         * Search step, in seconds (default 900 = 15min)
         */
        slot_step_seconds?: number;
    };
    url: '/calendar-groups/{id}/bookable-slots{format}';
};

export type CalendarGroupsBookableSlotsFormattedListResponses = {
    200: PaginatedBookableSlotProposalList;
};

export type CalendarGroupsBookableSlotsFormattedListResponse = CalendarGroupsBookableSlotsFormattedListResponses[keyof CalendarGroupsBookableSlotsFormattedListResponses];

export type CalendarGroupsBookedEventsListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query: {
        /**
         * End datetime in ISO format
         */
        end_datetime: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * Filter by partial name match
         */
        name?: string;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Start datetime in ISO format
         */
        start_datetime: string;
    };
    url: '/calendar-groups/{id}/booked-events/';
};

export type CalendarGroupsBookedEventsListResponses = {
    200: PaginatedCalendarEventList;
};

export type CalendarGroupsBookedEventsListResponse = CalendarGroupsBookedEventsListResponses[keyof CalendarGroupsBookedEventsListResponses];

export type CalendarGroupsBookedEventsFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query: {
        /**
         * End datetime in ISO format
         */
        end_datetime: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * Filter by partial name match
         */
        name?: string;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Start datetime in ISO format
         */
        start_datetime: string;
    };
    url: '/calendar-groups/{id}/booked-events{format}';
};

export type CalendarGroupsBookedEventsFormattedListResponses = {
    200: PaginatedCalendarEventList;
};

export type CalendarGroupsBookedEventsFormattedListResponse = CalendarGroupsBookedEventsFormattedListResponses[keyof CalendarGroupsBookedEventsFormattedListResponses];

export type CalendarGroupsEventsCreateData = {
    body: CalendarGroupEventCreate;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar-groups/{id}/events/';
};

export type CalendarGroupsEventsCreateResponses = {
    201: CalendarEvent;
};

export type CalendarGroupsEventsCreateResponse = CalendarGroupsEventsCreateResponses[keyof CalendarGroupsEventsCreateResponses];

export type CalendarGroupsEventsFormattedCreateData = {
    body: CalendarGroupEventCreate;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar-groups/{id}/events{format}';
};

export type CalendarGroupsEventsFormattedCreateResponses = {
    201: CalendarEvent;
};

export type CalendarGroupsEventsFormattedCreateResponse = CalendarGroupsEventsFormattedCreateResponses[keyof CalendarGroupsEventsFormattedCreateResponses];

export type CalendarDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar/{id}/';
};

export type CalendarDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type CalendarDestroyResponse = CalendarDestroyResponses[keyof CalendarDestroyResponses];

export type CalendarRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar/{id}/';
};

export type CalendarRetrieveResponses = {
    200: Calendar;
};

export type CalendarRetrieveResponse = CalendarRetrieveResponses[keyof CalendarRetrieveResponses];

export type CalendarPartialUpdateData = {
    body?: PatchedCalendarWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar/{id}/';
};

export type CalendarPartialUpdateResponses = {
    200: Calendar;
};

export type CalendarPartialUpdateResponse = CalendarPartialUpdateResponses[keyof CalendarPartialUpdateResponses];

export type CalendarUpdateData = {
    body: CalendarWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar/{id}/';
};

export type CalendarUpdateResponses = {
    200: Calendar;
};

export type CalendarUpdateResponse = CalendarUpdateResponses[keyof CalendarUpdateResponses];

export type CalendarFormattedDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar/{id}{format}';
};

export type CalendarFormattedDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type CalendarFormattedDestroyResponse = CalendarFormattedDestroyResponses[keyof CalendarFormattedDestroyResponses];

export type CalendarFormattedRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar/{id}{format}';
};

export type CalendarFormattedRetrieveResponses = {
    200: Calendar;
};

export type CalendarFormattedRetrieveResponse = CalendarFormattedRetrieveResponses[keyof CalendarFormattedRetrieveResponses];

export type CalendarFormattedPartialUpdateData = {
    body?: PatchedCalendarWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar/{id}{format}';
};

export type CalendarFormattedPartialUpdateResponses = {
    200: Calendar;
};

export type CalendarFormattedPartialUpdateResponse = CalendarFormattedPartialUpdateResponses[keyof CalendarFormattedPartialUpdateResponses];

export type CalendarFormattedUpdateData = {
    body: CalendarWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar/{id}{format}';
};

export type CalendarFormattedUpdateResponses = {
    200: Calendar;
};

export type CalendarFormattedUpdateResponse = CalendarFormattedUpdateResponses[keyof CalendarFormattedUpdateResponses];

export type CalendarAdminSyncCreateData = {
    body: CalendarSyncRequest;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar/{id}/admin-sync/';
};

export type CalendarAdminSyncCreateErrors = {
    /**
     * Sync is disabled for this calendar.
     */
    409: unknown;
};

export type CalendarAdminSyncCreateResponses = {
    202: CalendarSync;
};

export type CalendarAdminSyncCreateResponse = CalendarAdminSyncCreateResponses[keyof CalendarAdminSyncCreateResponses];

export type CalendarAdminSyncFormattedCreateData = {
    body: CalendarSyncRequest;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar/{id}/admin-sync{format}';
};

export type CalendarAdminSyncFormattedCreateErrors = {
    /**
     * Sync is disabled for this calendar.
     */
    409: unknown;
};

export type CalendarAdminSyncFormattedCreateResponses = {
    202: CalendarSync;
};

export type CalendarAdminSyncFormattedCreateResponse = CalendarAdminSyncFormattedCreateResponses[keyof CalendarAdminSyncFormattedCreateResponses];

export type CalendarAvailableWindowsListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query: {
        /**
         * Filter by calendar type (e.g. resource)
         *
         * * `personal` - Personal Calendar
         * * `resource` - Resource Calendar
         * * `virtual` - Virtual Calendar
         * * `bundle` - Bundle Calendar
         */
        calendar_type?: 'bundle' | 'personal' | 'resource' | 'virtual';
        /**
         * End datetime in ISO format (YYYY-MM-DDTHH:MM:SS)
         */
        end_datetime: string;
        /**
         * Filter by provider (internal = manual, others = synced)
         *
         * * `internal` - Internal Calendar
         * * `google` - Google Calendar
         * * `microsoft` - Microsoft Outlook Calendar
         * * `apple` - Apple Calendar
         * * `ics` - ICS
         */
        provider?: 'apple' | 'google' | 'ics' | 'internal' | 'microsoft';
        /**
         * Start datetime in ISO format (YYYY-MM-DDTHH:MM:SS)
         */
        start_datetime: string;
        /**
         * Filter by whether provider sync is enabled
         */
        sync_enabled?: boolean;
    };
    url: '/calendar/{id}/available-windows/';
};

export type CalendarAvailableWindowsListResponses = {
    200: Array<AvailableTimeWindow>;
};

export type CalendarAvailableWindowsListResponse = CalendarAvailableWindowsListResponses[keyof CalendarAvailableWindowsListResponses];

export type CalendarAvailableWindowsFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query: {
        /**
         * Filter by calendar type (e.g. resource)
         *
         * * `personal` - Personal Calendar
         * * `resource` - Resource Calendar
         * * `virtual` - Virtual Calendar
         * * `bundle` - Bundle Calendar
         */
        calendar_type?: 'bundle' | 'personal' | 'resource' | 'virtual';
        /**
         * End datetime in ISO format (YYYY-MM-DDTHH:MM:SS)
         */
        end_datetime: string;
        /**
         * Filter by provider (internal = manual, others = synced)
         *
         * * `internal` - Internal Calendar
         * * `google` - Google Calendar
         * * `microsoft` - Microsoft Outlook Calendar
         * * `apple` - Apple Calendar
         * * `ics` - ICS
         */
        provider?: 'apple' | 'google' | 'ics' | 'internal' | 'microsoft';
        /**
         * Start datetime in ISO format (YYYY-MM-DDTHH:MM:SS)
         */
        start_datetime: string;
        /**
         * Filter by whether provider sync is enabled
         */
        sync_enabled?: boolean;
    };
    url: '/calendar/{id}/available-windows{format}';
};

export type CalendarAvailableWindowsFormattedListResponses = {
    200: Array<AvailableTimeWindow>;
};

export type CalendarAvailableWindowsFormattedListResponse = CalendarAvailableWindowsFormattedListResponses[keyof CalendarAvailableWindowsFormattedListResponses];

export type CalendarBundlePartialUpdateData = {
    body?: PatchedCalendarBundleUpdate;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar/{id}/bundle/';
};

export type CalendarBundlePartialUpdateResponses = {
    200: Calendar;
};

export type CalendarBundlePartialUpdateResponse = CalendarBundlePartialUpdateResponses[keyof CalendarBundlePartialUpdateResponses];

export type CalendarBundleFormattedPartialUpdateData = {
    body?: PatchedCalendarBundleUpdate;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar/{id}/bundle{format}';
};

export type CalendarBundleFormattedPartialUpdateResponses = {
    200: Calendar;
};

export type CalendarBundleFormattedPartialUpdateResponse = CalendarBundleFormattedPartialUpdateResponses[keyof CalendarBundleFormattedPartialUpdateResponses];

export type CalendarRequestSyncCreateData = {
    body: CalendarSyncRequest;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/calendar/{id}/request-sync/';
};

export type CalendarRequestSyncCreateErrors = {
    /**
     * Sync is disabled for this calendar.
     */
    409: unknown;
};

export type CalendarRequestSyncCreateResponses = {
    202: CalendarSync;
};

export type CalendarRequestSyncCreateResponse = CalendarRequestSyncCreateResponses[keyof CalendarRequestSyncCreateResponses];

export type CalendarRequestSyncFormattedCreateData = {
    body: CalendarSyncRequest;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/calendar/{id}/request-sync{format}';
};

export type CalendarRequestSyncFormattedCreateErrors = {
    /**
     * Sync is disabled for this calendar.
     */
    409: unknown;
};

export type CalendarRequestSyncFormattedCreateResponses = {
    202: CalendarSync;
};

export type CalendarRequestSyncFormattedCreateResponse = CalendarRequestSyncFormattedCreateResponses[keyof CalendarRequestSyncFormattedCreateResponses];

export type CalendarUnavailableWindowsListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query: {
        /**
         * Filter by calendar type (e.g. resource)
         *
         * * `personal` - Personal Calendar
         * * `resource` - Resource Calendar
         * * `virtual` - Virtual Calendar
         * * `bundle` - Bundle Calendar
         */
        calendar_type?: 'bundle' | 'personal' | 'resource' | 'virtual';
        /**
         * End datetime in ISO format (YYYY-MM-DDTHH:MM:SS)
         */
        end_datetime: string;
        /**
         * Filter by provider (internal = manual, others = synced)
         *
         * * `internal` - Internal Calendar
         * * `google` - Google Calendar
         * * `microsoft` - Microsoft Outlook Calendar
         * * `apple` - Apple Calendar
         * * `ics` - ICS
         */
        provider?: 'apple' | 'google' | 'ics' | 'internal' | 'microsoft';
        /**
         * Start datetime in ISO format (YYYY-MM-DDTHH:MM:SS)
         */
        start_datetime: string;
        /**
         * Filter by whether provider sync is enabled
         */
        sync_enabled?: boolean;
    };
    url: '/calendar/{id}/unavailable-windows/';
};

export type CalendarUnavailableWindowsListResponses = {
    200: Array<UnavailableTimeWindow>;
};

export type CalendarUnavailableWindowsListResponse = CalendarUnavailableWindowsListResponses[keyof CalendarUnavailableWindowsListResponses];

export type CalendarUnavailableWindowsFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query: {
        /**
         * Filter by calendar type (e.g. resource)
         *
         * * `personal` - Personal Calendar
         * * `resource` - Resource Calendar
         * * `virtual` - Virtual Calendar
         * * `bundle` - Bundle Calendar
         */
        calendar_type?: 'bundle' | 'personal' | 'resource' | 'virtual';
        /**
         * End datetime in ISO format (YYYY-MM-DDTHH:MM:SS)
         */
        end_datetime: string;
        /**
         * Filter by provider (internal = manual, others = synced)
         *
         * * `internal` - Internal Calendar
         * * `google` - Google Calendar
         * * `microsoft` - Microsoft Outlook Calendar
         * * `apple` - Apple Calendar
         * * `ics` - ICS
         */
        provider?: 'apple' | 'google' | 'ics' | 'internal' | 'microsoft';
        /**
         * Start datetime in ISO format (YYYY-MM-DDTHH:MM:SS)
         */
        start_datetime: string;
        /**
         * Filter by whether provider sync is enabled
         */
        sync_enabled?: boolean;
    };
    url: '/calendar/{id}/unavailable-windows{format}';
};

export type CalendarUnavailableWindowsFormattedListResponses = {
    200: Array<UnavailableTimeWindow>;
};

export type CalendarUnavailableWindowsFormattedListResponse = CalendarUnavailableWindowsFormattedListResponses[keyof CalendarUnavailableWindowsFormattedListResponses];

export type CalendarBundleCreateData = {
    body: CalendarBundleCreate;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/calendar/bundle/';
};

export type CalendarBundleCreateResponses = {
    200: Calendar;
};

export type CalendarBundleCreateResponse = CalendarBundleCreateResponses[keyof CalendarBundleCreateResponses];

export type CalendarBundleFormattedCreateData = {
    body: CalendarBundleCreate;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: never;
    url: '/calendar/bundle{format}';
};

export type CalendarBundleFormattedCreateResponses = {
    200: Calendar;
};

export type CalendarBundleFormattedCreateResponse = CalendarBundleFormattedCreateResponses[keyof CalendarBundleFormattedCreateResponses];

export type CalendarDefaultRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/calendar/default/';
};

export type CalendarDefaultRetrieveErrors = {
    /**
     * No default calendar for this user
     */
    404: unknown;
};

export type CalendarDefaultRetrieveResponses = {
    200: Calendar;
};

export type CalendarDefaultRetrieveResponse = CalendarDefaultRetrieveResponses[keyof CalendarDefaultRetrieveResponses];

export type CalendarDefaultFormattedRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: never;
    url: '/calendar/default{format}';
};

export type CalendarDefaultFormattedRetrieveErrors = {
    /**
     * No default calendar for this user
     */
    404: unknown;
};

export type CalendarDefaultFormattedRetrieveResponses = {
    200: Calendar;
};

export type CalendarDefaultFormattedRetrieveResponse = CalendarDefaultFormattedRetrieveResponses[keyof CalendarDefaultFormattedRetrieveResponses];

export type CalendarRequestImportCreateData = {
    body: CalendarWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/calendar/request-import/';
};

export type CalendarRequestImportCreateResponses = {
    202: {
        detail?: string;
    };
};

export type CalendarRequestImportCreateResponse = CalendarRequestImportCreateResponses[keyof CalendarRequestImportCreateResponses];

export type CalendarRequestImportFormattedCreateData = {
    body: CalendarWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: never;
    url: '/calendar/request-import{format}';
};

export type CalendarRequestImportFormattedCreateResponses = {
    202: {
        detail?: string;
    };
};

export type CalendarRequestImportFormattedCreateResponse = CalendarRequestImportFormattedCreateResponses[keyof CalendarRequestImportFormattedCreateResponses];

export type CalendarResourceCreateData = {
    body: ResourceCalendarCreate;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/calendar/resource/';
};

export type CalendarResourceCreateResponses = {
    201: Calendar;
};

export type CalendarResourceCreateResponse = CalendarResourceCreateResponses[keyof CalendarResourceCreateResponses];

export type CalendarResourceFormattedCreateData = {
    body: ResourceCalendarCreate;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: never;
    url: '/calendar/resource{format}';
};

export type CalendarResourceFormattedCreateResponses = {
    201: Calendar;
};

export type CalendarResourceFormattedCreateResponse = CalendarResourceFormattedCreateResponses[keyof CalendarResourceFormattedCreateResponses];

export type InvitationsListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Filter by partial email match
         */
        email?: string;
        /**
         * Filter by inviter user ID
         */
        invited_by?: number;
        /**
         * Filter by acceptance status
         */
        is_accepted?: boolean;
        /**
         * Filter by expiration status
         */
        is_expired?: boolean;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/invitations/';
};

export type InvitationsListResponses = {
    200: PaginatedOrganizationInvitationList;
};

export type InvitationsListResponse = InvitationsListResponses[keyof InvitationsListResponses];

export type InvitationsCreateData = {
    body: OrganizationInvitationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/invitations/';
};

export type InvitationsCreateResponses = {
    201: OrganizationInvitation;
};

export type InvitationsCreateResponse = InvitationsCreateResponses[keyof InvitationsCreateResponses];

export type InvitationsFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: {
        /**
         * Filter by partial email match
         */
        email?: string;
        /**
         * Filter by inviter user ID
         */
        invited_by?: number;
        /**
         * Filter by acceptance status
         */
        is_accepted?: boolean;
        /**
         * Filter by expiration status
         */
        is_expired?: boolean;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/invitations{format}';
};

export type InvitationsFormattedListResponses = {
    200: PaginatedOrganizationInvitationList;
};

export type InvitationsFormattedListResponse = InvitationsFormattedListResponses[keyof InvitationsFormattedListResponses];

export type InvitationsFormattedCreateData = {
    body: OrganizationInvitationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: never;
    url: '/invitations{format}';
};

export type InvitationsFormattedCreateResponses = {
    201: OrganizationInvitation;
};

export type InvitationsFormattedCreateResponse = InvitationsFormattedCreateResponses[keyof InvitationsFormattedCreateResponses];

export type InvitationsDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/invitations/{id}/';
};

export type InvitationsDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type InvitationsDestroyResponse = InvitationsDestroyResponses[keyof InvitationsDestroyResponses];

export type InvitationsRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/invitations/{id}/';
};

export type InvitationsRetrieveResponses = {
    200: OrganizationInvitation;
};

export type InvitationsRetrieveResponse = InvitationsRetrieveResponses[keyof InvitationsRetrieveResponses];

export type InvitationsFormattedDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/invitations/{id}{format}';
};

export type InvitationsFormattedDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type InvitationsFormattedDestroyResponse = InvitationsFormattedDestroyResponses[keyof InvitationsFormattedDestroyResponses];

export type InvitationsFormattedRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/invitations/{id}{format}';
};

export type InvitationsFormattedRetrieveResponses = {
    200: OrganizationInvitation;
};

export type InvitationsFormattedRetrieveResponse = InvitationsFormattedRetrieveResponses[keyof InvitationsFormattedRetrieveResponses];

export type InvitationsResendCreateData = {
    body: OrganizationInvitationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/invitations/{id}/resend/';
};

export type InvitationsResendCreateErrors = {
    /**
     * Invitation already accepted or service error
     */
    400: unknown;
    /**
     * Not an active member
     */
    403: unknown;
    /**
     * Invitation not found or cross-org
     */
    404: unknown;
};

export type InvitationsResendCreateResponses = {
    200: OrganizationInvitation;
};

export type InvitationsResendCreateResponse = InvitationsResendCreateResponses[keyof InvitationsResendCreateResponses];

export type InvitationsResendFormattedCreateData = {
    body: OrganizationInvitationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/invitations/{id}/resend{format}';
};

export type InvitationsResendFormattedCreateErrors = {
    /**
     * Invitation already accepted or service error
     */
    400: unknown;
    /**
     * Not an active member
     */
    403: unknown;
    /**
     * Invitation not found or cross-org
     */
    404: unknown;
};

export type InvitationsResendFormattedCreateResponses = {
    200: OrganizationInvitation;
};

export type InvitationsResendFormattedCreateResponse = InvitationsResendFormattedCreateResponses[keyof InvitationsResendFormattedCreateResponses];

export type InvitationsAcceptCreateData = {
    body: AcceptInvitation;
    path?: never;
    query?: never;
    url: '/invitations/accept';
};

export type InvitationsAcceptCreateResponses = {
    201: AcceptInvitation;
};

export type InvitationsAcceptCreateResponse = InvitationsAcceptCreateResponses[keyof InvitationsAcceptCreateResponses];

export type NotificationsListData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * Page number (1-based). Defaults to 1.
         */
        page?: number;
        /**
         * Number of items per page. Defaults to 10, max 100.
         */
        page_size?: number;
    };
    url: '/notifications/';
};

export type NotificationsListErrors = {
    /**
     * Invalid page / page_size parameter
     */
    400: unknown;
    /**
     * Unauthenticated
     */
    401: unknown;
};

export type NotificationsListResponses = {
    /**
     * Passthrough-paginated list of all in-app notifications. Envelope: {results: [...], page: int, page_size: int, count: int}.
     */
    200: unknown;
};

export type NotificationsFormattedListData = {
    body?: never;
    path: {
        format: '.json';
    };
    query?: {
        /**
         * Page number (1-based). Defaults to 1.
         */
        page?: number;
        /**
         * Number of items per page. Defaults to 10, max 100.
         */
        page_size?: number;
    };
    url: '/notifications{format}';
};

export type NotificationsFormattedListErrors = {
    /**
     * Invalid page / page_size parameter
     */
    400: unknown;
    /**
     * Unauthenticated
     */
    401: unknown;
};

export type NotificationsFormattedListResponses = {
    /**
     * Passthrough-paginated list of all in-app notifications. Envelope: {results: [...], page: int, page_size: int, count: int}.
     */
    200: unknown;
};

export type NotificationsMarkReadCreateData = {
    body?: Notification;
    path: {
        id: string;
    };
    query?: never;
    url: '/notifications/{id}/mark-read/';
};

export type NotificationsMarkReadCreateErrors = {
    /**
     * Unauthenticated
     */
    401: unknown;
    /**
     * Notification not found or not owned by the authenticated user
     */
    404: unknown;
};

export type NotificationsMarkReadCreateResponses = {
    /**
     * Notification marked as read. Returns the updated notification.
     */
    200: Notification;
};

export type NotificationsMarkReadCreateResponse = NotificationsMarkReadCreateResponses[keyof NotificationsMarkReadCreateResponses];

export type NotificationsMarkReadFormattedCreateData = {
    body?: Notification;
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/notifications/{id}/mark-read{format}';
};

export type NotificationsMarkReadFormattedCreateErrors = {
    /**
     * Unauthenticated
     */
    401: unknown;
    /**
     * Notification not found or not owned by the authenticated user
     */
    404: unknown;
};

export type NotificationsMarkReadFormattedCreateResponses = {
    /**
     * Notification marked as read. Returns the updated notification.
     */
    200: Notification;
};

export type NotificationsMarkReadFormattedCreateResponse = NotificationsMarkReadFormattedCreateResponses[keyof NotificationsMarkReadFormattedCreateResponses];

export type NotificationsMarkReadBulkCreateData = {
    body: BulkMarkRead;
    path?: never;
    query?: never;
    url: '/notifications/mark-read-bulk/';
};

export type NotificationsMarkReadBulkCreateErrors = {
    /**
     * Invalid request body (empty or missing ids list)
     */
    400: unknown;
    /**
     * Unauthenticated
     */
    401: unknown;
};

export type NotificationsMarkReadBulkCreateResponses = {
    /**
     * Notifications marked as read. Envelope: {results: [...serialized notifications that are READ after the op...]}.
     */
    200: unknown;
};

export type NotificationsMarkReadBulkFormattedCreateData = {
    body: BulkMarkRead;
    path: {
        format: '.json';
    };
    query?: never;
    url: '/notifications/mark-read-bulk{format}';
};

export type NotificationsMarkReadBulkFormattedCreateErrors = {
    /**
     * Invalid request body (empty or missing ids list)
     */
    400: unknown;
    /**
     * Unauthenticated
     */
    401: unknown;
};

export type NotificationsMarkReadBulkFormattedCreateResponses = {
    /**
     * Notifications marked as read. Envelope: {results: [...serialized notifications that are READ after the op...]}.
     */
    200: unknown;
};

export type NotificationsUnreadRetrieveData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * Page number (1-based). Defaults to 1.
         */
        page?: number;
        /**
         * Number of items per page. Defaults to 10.
         */
        page_size?: number;
    };
    url: '/notifications/unread/';
};

export type NotificationsUnreadRetrieveErrors = {
    /**
     * Invalid page / page_size parameter
     */
    400: unknown;
    /**
     * Unauthenticated
     */
    401: unknown;
};

export type NotificationsUnreadRetrieveResponses = {
    /**
     * Passthrough-paginated list of unread notifications. Envelope: {results: [...], page: int, page_size: int, count: int}. count reflects the total number of unread notifications for the user.
     */
    200: unknown;
};

export type NotificationsUnreadFormattedRetrieveData = {
    body?: never;
    path: {
        format: '.json';
    };
    query?: {
        /**
         * Page number (1-based). Defaults to 1.
         */
        page?: number;
        /**
         * Number of items per page. Defaults to 10.
         */
        page_size?: number;
    };
    url: '/notifications/unread{format}';
};

export type NotificationsUnreadFormattedRetrieveErrors = {
    /**
     * Invalid page / page_size parameter
     */
    400: unknown;
    /**
     * Unauthenticated
     */
    401: unknown;
};

export type NotificationsUnreadFormattedRetrieveResponses = {
    /**
     * Passthrough-paginated list of unread notifications. Envelope: {results: [...], page: int, page_size: int, count: int}. count reflects the total number of unread notifications for the user.
     */
    200: unknown;
};

export type OrganizationMembersListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Filter by partial email match
         */
        email?: string;
        /**
         * Filter by partial first name match
         */
        first_name?: string;
        /**
         * Filter by partial last name match
         */
        last_name?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Search by first name, last name, or email (OR)
         */
        search?: string;
    };
    url: '/organization-members/';
};

export type OrganizationMembersListResponses = {
    200: PaginatedOrganizationMembershipList;
};

export type OrganizationMembersListResponse = OrganizationMembersListResponses[keyof OrganizationMembersListResponses];

export type OrganizationMembersFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: {
        /**
         * Filter by partial email match
         */
        email?: string;
        /**
         * Filter by partial first name match
         */
        first_name?: string;
        /**
         * Filter by partial last name match
         */
        last_name?: string;
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
        /**
         * Search by first name, last name, or email (OR)
         */
        search?: string;
    };
    url: '/organization-members{format}';
};

export type OrganizationMembersFormattedListResponses = {
    200: PaginatedOrganizationMembershipList;
};

export type OrganizationMembersFormattedListResponse = OrganizationMembersFormattedListResponses[keyof OrganizationMembersFormattedListResponses];

export type OrganizationMembersRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        user_id: string;
    };
    query?: never;
    url: '/organization-members/{user_id}/';
};

export type OrganizationMembersRetrieveResponses = {
    200: OrganizationMembership;
};

export type OrganizationMembersRetrieveResponse = OrganizationMembersRetrieveResponses[keyof OrganizationMembersRetrieveResponses];

export type OrganizationMembersFormattedRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        user_id: string;
    };
    query?: never;
    url: '/organization-members/{user_id}{format}';
};

export type OrganizationMembersFormattedRetrieveResponses = {
    200: OrganizationMembership;
};

export type OrganizationMembersFormattedRetrieveResponse = OrganizationMembersFormattedRetrieveResponses[keyof OrganizationMembersFormattedRetrieveResponses];

export type OrganizationMembersDeactivateCreateData = {
    body?: OrganizationMembership;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        user_id: string;
    };
    query?: never;
    url: '/organization-members/{user_id}/deactivate/';
};

export type OrganizationMembersDeactivateCreateErrors = {
    /**
     * Cannot deactivate self or last active admin
     */
    400: unknown;
    /**
     * Not an admin
     */
    403: unknown;
    /**
     * Member not found or cross-org
     */
    404: unknown;
};

export type OrganizationMembersDeactivateCreateResponses = {
    200: OrganizationMembership;
};

export type OrganizationMembersDeactivateCreateResponse = OrganizationMembersDeactivateCreateResponses[keyof OrganizationMembersDeactivateCreateResponses];

export type OrganizationMembersDeactivateFormattedCreateData = {
    body?: OrganizationMembership;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        user_id: string;
    };
    query?: never;
    url: '/organization-members/{user_id}/deactivate{format}';
};

export type OrganizationMembersDeactivateFormattedCreateErrors = {
    /**
     * Cannot deactivate self or last active admin
     */
    400: unknown;
    /**
     * Not an admin
     */
    403: unknown;
    /**
     * Member not found or cross-org
     */
    404: unknown;
};

export type OrganizationMembersDeactivateFormattedCreateResponses = {
    200: OrganizationMembership;
};

export type OrganizationMembersDeactivateFormattedCreateResponse = OrganizationMembersDeactivateFormattedCreateResponses[keyof OrganizationMembersDeactivateFormattedCreateResponses];

export type OrganizationMembersReactivateCreateData = {
    body?: OrganizationMembership;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        user_id: string;
    };
    query?: never;
    url: '/organization-members/{user_id}/reactivate/';
};

export type OrganizationMembersReactivateCreateErrors = {
    /**
     * Not an admin
     */
    403: unknown;
    /**
     * Member not found or cross-org
     */
    404: unknown;
};

export type OrganizationMembersReactivateCreateResponses = {
    200: OrganizationMembership;
};

export type OrganizationMembersReactivateCreateResponse = OrganizationMembersReactivateCreateResponses[keyof OrganizationMembersReactivateCreateResponses];

export type OrganizationMembersReactivateFormattedCreateData = {
    body?: OrganizationMembership;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        user_id: string;
    };
    query?: never;
    url: '/organization-members/{user_id}/reactivate{format}';
};

export type OrganizationMembersReactivateFormattedCreateErrors = {
    /**
     * Not an admin
     */
    403: unknown;
    /**
     * Member not found or cross-org
     */
    404: unknown;
};

export type OrganizationMembersReactivateFormattedCreateResponses = {
    200: OrganizationMembership;
};

export type OrganizationMembersReactivateFormattedCreateResponse = OrganizationMembersReactivateFormattedCreateResponses[keyof OrganizationMembersReactivateFormattedCreateResponses];

export type OrganizationMembersUpdateRoleCreateData = {
    body: UpdateMembershipRole;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        user_id: string;
    };
    query?: never;
    url: '/organization-members/{user_id}/update-role/';
};

export type OrganizationMembersUpdateRoleCreateErrors = {
    /**
     * Invalid role or would demote the last active admin
     */
    400: unknown;
    /**
     * Not an admin
     */
    403: unknown;
    /**
     * Member not found or cross-org
     */
    404: unknown;
};

export type OrganizationMembersUpdateRoleCreateResponses = {
    200: OrganizationMembership;
};

export type OrganizationMembersUpdateRoleCreateResponse = OrganizationMembersUpdateRoleCreateResponses[keyof OrganizationMembersUpdateRoleCreateResponses];

export type OrganizationMembersUpdateRoleFormattedCreateData = {
    body: UpdateMembershipRole;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        user_id: string;
    };
    query?: never;
    url: '/organization-members/{user_id}/update-role{format}';
};

export type OrganizationMembersUpdateRoleFormattedCreateErrors = {
    /**
     * Invalid role or would demote the last active admin
     */
    400: unknown;
    /**
     * Not an admin
     */
    403: unknown;
    /**
     * Member not found or cross-org
     */
    404: unknown;
};

export type OrganizationMembersUpdateRoleFormattedCreateResponses = {
    200: OrganizationMembership;
};

export type OrganizationMembersUpdateRoleFormattedCreateResponse = OrganizationMembersUpdateRoleFormattedCreateResponses[keyof OrganizationMembersUpdateRoleFormattedCreateResponses];

export type OrganizationsCreateData = {
    body: OrganizationWritable;
    path?: never;
    query?: never;
    url: '/organizations/';
};

export type OrganizationsCreateResponses = {
    201: Organization;
};

export type OrganizationsCreateResponse = OrganizationsCreateResponses[keyof OrganizationsCreateResponses];

export type OrganizationsFormattedCreateData = {
    body: OrganizationWritable;
    path: {
        format: '.json';
    };
    query?: never;
    url: '/organizations{format}';
};

export type OrganizationsFormattedCreateResponses = {
    201: Organization;
};

export type OrganizationsFormattedCreateResponse = OrganizationsFormattedCreateResponses[keyof OrganizationsFormattedCreateResponses];

export type OrganizationsDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/organizations/{id}/';
};

export type OrganizationsDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type OrganizationsDestroyResponse = OrganizationsDestroyResponses[keyof OrganizationsDestroyResponses];

export type OrganizationsRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/organizations/{id}/';
};

export type OrganizationsRetrieveResponses = {
    200: Organization;
};

export type OrganizationsRetrieveResponse = OrganizationsRetrieveResponses[keyof OrganizationsRetrieveResponses];

export type OrganizationsPartialUpdateData = {
    body?: PatchedOrganizationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/organizations/{id}/';
};

export type OrganizationsPartialUpdateResponses = {
    200: Organization;
};

export type OrganizationsPartialUpdateResponse = OrganizationsPartialUpdateResponses[keyof OrganizationsPartialUpdateResponses];

export type OrganizationsUpdateData = {
    body: OrganizationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/organizations/{id}/';
};

export type OrganizationsUpdateResponses = {
    200: Organization;
};

export type OrganizationsUpdateResponse = OrganizationsUpdateResponses[keyof OrganizationsUpdateResponses];

export type OrganizationsFormattedDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/organizations/{id}{format}';
};

export type OrganizationsFormattedDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type OrganizationsFormattedDestroyResponse = OrganizationsFormattedDestroyResponses[keyof OrganizationsFormattedDestroyResponses];

export type OrganizationsFormattedRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/organizations/{id}{format}';
};

export type OrganizationsFormattedRetrieveResponses = {
    200: Organization;
};

export type OrganizationsFormattedRetrieveResponse = OrganizationsFormattedRetrieveResponses[keyof OrganizationsFormattedRetrieveResponses];

export type OrganizationsFormattedPartialUpdateData = {
    body?: PatchedOrganizationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/organizations/{id}{format}';
};

export type OrganizationsFormattedPartialUpdateResponses = {
    200: Organization;
};

export type OrganizationsFormattedPartialUpdateResponse = OrganizationsFormattedPartialUpdateResponses[keyof OrganizationsFormattedPartialUpdateResponses];

export type OrganizationsFormattedUpdateData = {
    body: OrganizationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/organizations/{id}{format}';
};

export type OrganizationsFormattedUpdateResponses = {
    200: Organization;
};

export type OrganizationsFormattedUpdateResponse = OrganizationsFormattedUpdateResponses[keyof OrganizationsFormattedUpdateResponses];

export type OrganizationsSyncCalendarsCreateData = {
    body: CalendarSyncRequest;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/organizations/{id}/sync-calendars/';
};

export type OrganizationsSyncCalendarsCreateErrors = {
    /**
     * Invalid sync window
     */
    400: unknown;
    /**
     * Not an admin
     */
    403: unknown;
    /**
     * Organization not found
     */
    404: unknown;
};

export type OrganizationsSyncCalendarsCreateResponses = {
    /**
     * Sync enqueued. Body: {synced: [calendar_id, ...], skipped: [{calendar_id, reason}, ...]}.
     */
    202: unknown;
};

export type OrganizationsSyncCalendarsFormattedCreateData = {
    body: CalendarSyncRequest;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/organizations/{id}/sync-calendars{format}';
};

export type OrganizationsSyncCalendarsFormattedCreateErrors = {
    /**
     * Invalid sync window
     */
    400: unknown;
    /**
     * Not an admin
     */
    403: unknown;
    /**
     * Organization not found
     */
    404: unknown;
};

export type OrganizationsSyncCalendarsFormattedCreateResponses = {
    /**
     * Sync enqueued. Body: {synced: [calendar_id, ...], skipped: [{calendar_id, reason}, ...]}.
     */
    202: unknown;
};

export type OrganizationsSyncRoomsCreateData = {
    body: OrganizationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/organizations/{id}/sync-rooms/';
};

export type OrganizationsSyncRoomsCreateErrors = {
    /**
     * Invalid datetime format
     */
    400: unknown;
    /**
     * Not an admin
     */
    403: unknown;
    /**
     * Organization not found
     */
    404: unknown;
};

export type OrganizationsSyncRoomsCreateResponses = {
    202: Organization;
};

export type OrganizationsSyncRoomsCreateResponse = OrganizationsSyncRoomsCreateResponses[keyof OrganizationsSyncRoomsCreateResponses];

export type OrganizationsSyncRoomsFormattedCreateData = {
    body: OrganizationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/organizations/{id}/sync-rooms{format}';
};

export type OrganizationsSyncRoomsFormattedCreateErrors = {
    /**
     * Invalid datetime format
     */
    400: unknown;
    /**
     * Not an admin
     */
    403: unknown;
    /**
     * Organization not found
     */
    404: unknown;
};

export type OrganizationsSyncRoomsFormattedCreateResponses = {
    202: Organization;
};

export type OrganizationsSyncRoomsFormattedCreateResponse = OrganizationsSyncRoomsFormattedCreateResponses[keyof OrganizationsSyncRoomsFormattedCreateResponses];

export type OrganizationsCurrentRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/organizations/current/';
};

export type OrganizationsCurrentRetrieveErrors = {
    /**
     * No organization membership (gated user)
     */
    404: unknown;
};

export type OrganizationsCurrentRetrieveResponses = {
    200: CurrentMembership;
};

export type OrganizationsCurrentRetrieveResponse = OrganizationsCurrentRetrieveResponses[keyof OrganizationsCurrentRetrieveResponses];

export type OrganizationsCurrentFormattedRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: never;
    url: '/organizations/current{format}';
};

export type OrganizationsCurrentFormattedRetrieveErrors = {
    /**
     * No organization membership (gated user)
     */
    404: unknown;
};

export type OrganizationsCurrentFormattedRetrieveResponses = {
    200: CurrentMembership;
};

export type OrganizationsCurrentFormattedRetrieveResponse = OrganizationsCurrentFormattedRetrieveResponses[keyof OrganizationsCurrentFormattedRetrieveResponses];

export type OrganizationsMineListData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/organizations/mine/';
};

export type OrganizationsMineListResponses = {
    200: Array<MyMembership>;
};

export type OrganizationsMineListResponse = OrganizationsMineListResponses[keyof OrganizationsMineListResponses];

export type OrganizationsMineFormattedListData = {
    body?: never;
    path: {
        format: '.json';
    };
    query?: never;
    url: '/organizations/mine{format}';
};

export type OrganizationsMineFormattedListResponses = {
    200: Array<MyMembership>;
};

export type OrganizationsMineFormattedListResponse = OrganizationsMineFormattedListResponses[keyof OrganizationsMineFormattedListResponses];

export type PaymentsPaymentUpdateCreateData = {
    body?: never;
    path: {
        id: string;
        provider: string;
    };
    query?: never;
    url: '/payments/{id}/payment-update/{provider}/';
};

export type PaymentsPaymentUpdateCreateResponses = {
    /**
     * Payment update received.
     */
    200: unknown;
};

export type PaymentsPaymentUpdateFormattedCreateData = {
    body?: never;
    path: {
        format: '.json';
        id: string;
        provider: string;
    };
    query?: never;
    url: '/payments/{id}/payment-update/{provider}{format}';
};

export type PaymentsPaymentUpdateFormattedCreateResponses = {
    /**
     * Payment update received.
     */
    200: unknown;
};

export type PaymentsSubscriptionPaymentUpdateCreateData = {
    body?: never;
    path: {
        id: string;
        provider: string;
    };
    query?: never;
    url: '/payments/{id}/subscription-payment-update/{provider}/';
};

export type PaymentsSubscriptionPaymentUpdateCreateResponses = {
    /**
     * Subscription payment update received.
     */
    200: unknown;
};

export type PaymentsSubscriptionPaymentUpdateFormattedCreateData = {
    body?: never;
    path: {
        format: '.json';
        id: string;
        provider: string;
    };
    query?: never;
    url: '/payments/{id}/subscription-payment-update/{provider}{format}';
};

export type PaymentsSubscriptionPaymentUpdateFormattedCreateResponses = {
    /**
     * Subscription payment update received.
     */
    200: unknown;
};

export type ProfileRetrieveData = {
    body?: never;
    path: {
        /**
         * User ID to retrieve or update the profile. Use 'me' to refer to the currently authenticated user.
         */
        user: string;
    };
    query?: never;
    url: '/profile/{user}/';
};

export type ProfileRetrieveResponses = {
    200: Profile;
};

export type ProfileRetrieveResponse = ProfileRetrieveResponses[keyof ProfileRetrieveResponses];

export type ProfilePartialUpdateData = {
    body?: PatchedProfileWritable;
    path: {
        /**
         * User ID to update the profile. Use 'me' to refer to the currently authenticated user.
         */
        user: string;
    };
    query?: never;
    url: '/profile/{user}/';
};

export type ProfilePartialUpdateResponses = {
    200: Profile;
};

export type ProfilePartialUpdateResponse = ProfilePartialUpdateResponses[keyof ProfilePartialUpdateResponses];

export type ProfileUpdateData = {
    body?: ProfileWritable;
    path: {
        /**
         * User ID to update the profile. Use 'me' to refer to the currently authenticated user.
         */
        user: string;
    };
    query?: never;
    url: '/profile/{user}/';
};

export type ProfileUpdateResponses = {
    200: Profile;
};

export type ProfileUpdateResponse = ProfileUpdateResponses[keyof ProfileUpdateResponses];

export type ProfileFormattedRetrieveData = {
    body?: never;
    path: {
        format: '.json';
        /**
         * User ID to retrieve or update the profile. Use 'me' to refer to the currently authenticated user.
         */
        user: string;
    };
    query?: never;
    url: '/profile/{user}{format}';
};

export type ProfileFormattedRetrieveResponses = {
    200: Profile;
};

export type ProfileFormattedRetrieveResponse = ProfileFormattedRetrieveResponses[keyof ProfileFormattedRetrieveResponses];

export type ProfileFormattedPartialUpdateData = {
    body?: PatchedProfileWritable;
    path: {
        format: '.json';
        /**
         * User ID to update the profile. Use 'me' to refer to the currently authenticated user.
         */
        user: string;
    };
    query?: never;
    url: '/profile/{user}{format}';
};

export type ProfileFormattedPartialUpdateResponses = {
    200: Profile;
};

export type ProfileFormattedPartialUpdateResponse = ProfileFormattedPartialUpdateResponses[keyof ProfileFormattedPartialUpdateResponses];

export type ProfileFormattedUpdateData = {
    body?: ProfileWritable;
    path: {
        format: '.json';
        /**
         * User ID to update the profile. Use 'me' to refer to the currently authenticated user.
         */
        user: string;
    };
    query?: never;
    url: '/profile/{user}{format}';
};

export type ProfileFormattedUpdateResponses = {
    200: Profile;
};

export type ProfileFormattedUpdateResponse = ProfileFormattedUpdateResponses[keyof ProfileFormattedUpdateResponses];

export type ProfileProfilePictureUploadParamsCreateData = {
    body: ProfilePictureUploadParamsRequest;
    path: {
        /**
         * User ID. Use 'me' to refer to the currently authenticated user.
         */
        user: string;
    };
    query?: never;
    url: '/profile/{user}/profile-picture-upload-params/';
};

export type ProfileProfilePictureUploadParamsCreateResponses = {
    200: ProfilePictureUploadParams;
};

export type ProfileProfilePictureUploadParamsCreateResponse = ProfileProfilePictureUploadParamsCreateResponses[keyof ProfileProfilePictureUploadParamsCreateResponses];

export type ProfileProfilePictureUploadParamsFormattedCreateData = {
    body: ProfilePictureUploadParamsRequest;
    path: {
        format: '.json';
        /**
         * User ID. Use 'me' to refer to the currently authenticated user.
         */
        user: string;
    };
    query?: never;
    url: '/profile/{user}/profile-picture-upload-params{format}';
};

export type ProfileProfilePictureUploadParamsFormattedCreateResponses = {
    200: ProfilePictureUploadParams;
};

export type ProfileProfilePictureUploadParamsFormattedCreateResponse = ProfileProfilePictureUploadParamsFormattedCreateResponses[keyof ProfileProfilePictureUploadParamsFormattedCreateResponses];

export type PublicApiTokensListData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/public-api-tokens/';
};

export type PublicApiTokensListResponses = {
    200: PaginatedSystemUserTokenList;
};

export type PublicApiTokensListResponse = PublicApiTokensListResponses[keyof PublicApiTokensListResponses];

export type PublicApiTokensCreateData = {
    body: SystemUserTokenCreate;
    path?: never;
    query?: never;
    url: '/public-api-tokens/';
};

export type PublicApiTokensCreateResponses = {
    201: SystemUserTokenResponse;
};

export type PublicApiTokensCreateResponse = PublicApiTokensCreateResponses[keyof PublicApiTokensCreateResponses];

export type PublicApiTokensFormattedListData = {
    body?: never;
    path: {
        format: '.json';
    };
    query?: {
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/public-api-tokens{format}';
};

export type PublicApiTokensFormattedListResponses = {
    200: PaginatedSystemUserTokenList;
};

export type PublicApiTokensFormattedListResponse = PublicApiTokensFormattedListResponses[keyof PublicApiTokensFormattedListResponses];

export type PublicApiTokensFormattedCreateData = {
    body: SystemUserTokenCreate;
    path: {
        format: '.json';
    };
    query?: never;
    url: '/public-api-tokens{format}';
};

export type PublicApiTokensFormattedCreateResponses = {
    201: SystemUserTokenResponse;
};

export type PublicApiTokensFormattedCreateResponse = PublicApiTokensFormattedCreateResponses[keyof PublicApiTokensFormattedCreateResponses];

export type PublicApiTokensRetrieveData = {
    body?: never;
    path: {
        id: string;
    };
    query?: never;
    url: '/public-api-tokens/{id}/';
};

export type PublicApiTokensRetrieveResponses = {
    200: SystemUserToken;
};

export type PublicApiTokensRetrieveResponse = PublicApiTokensRetrieveResponses[keyof PublicApiTokensRetrieveResponses];

export type PublicApiTokensPartialUpdateData = {
    body?: PatchedSystemUserTokenUpdate;
    path: {
        id: string;
    };
    query?: never;
    url: '/public-api-tokens/{id}/';
};

export type PublicApiTokensPartialUpdateResponses = {
    200: SystemUserToken;
};

export type PublicApiTokensPartialUpdateResponse = PublicApiTokensPartialUpdateResponses[keyof PublicApiTokensPartialUpdateResponses];

export type PublicApiTokensUpdateData = {
    body: SystemUserTokenUpdate;
    path: {
        id: string;
    };
    query?: never;
    url: '/public-api-tokens/{id}/';
};

export type PublicApiTokensUpdateResponses = {
    200: SystemUserToken;
};

export type PublicApiTokensUpdateResponse = PublicApiTokensUpdateResponses[keyof PublicApiTokensUpdateResponses];

export type PublicApiTokensFormattedRetrieveData = {
    body?: never;
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/public-api-tokens/{id}{format}';
};

export type PublicApiTokensFormattedRetrieveResponses = {
    200: SystemUserToken;
};

export type PublicApiTokensFormattedRetrieveResponse = PublicApiTokensFormattedRetrieveResponses[keyof PublicApiTokensFormattedRetrieveResponses];

export type PublicApiTokensFormattedPartialUpdateData = {
    body?: PatchedSystemUserTokenUpdate;
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/public-api-tokens/{id}{format}';
};

export type PublicApiTokensFormattedPartialUpdateResponses = {
    200: SystemUserToken;
};

export type PublicApiTokensFormattedPartialUpdateResponse = PublicApiTokensFormattedPartialUpdateResponses[keyof PublicApiTokensFormattedPartialUpdateResponses];

export type PublicApiTokensFormattedUpdateData = {
    body: SystemUserTokenUpdate;
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/public-api-tokens/{id}{format}';
};

export type PublicApiTokensFormattedUpdateResponses = {
    200: SystemUserToken;
};

export type PublicApiTokensFormattedUpdateResponse = PublicApiTokensFormattedUpdateResponses[keyof PublicApiTokensFormattedUpdateResponses];

export type PublicApiTokensRevokeCreateData = {
    body?: SystemUserToken;
    path: {
        id: string;
    };
    query?: never;
    url: '/public-api-tokens/{id}/revoke/';
};

export type PublicApiTokensRevokeCreateResponses = {
    200: SystemUserToken;
};

export type PublicApiTokensRevokeCreateResponse = PublicApiTokensRevokeCreateResponses[keyof PublicApiTokensRevokeCreateResponses];

export type PublicApiTokensRevokeFormattedCreateData = {
    body?: SystemUserToken;
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/public-api-tokens/{id}/revoke{format}';
};

export type PublicApiTokensRevokeFormattedCreateResponses = {
    200: SystemUserToken;
};

export type PublicApiTokensRevokeFormattedCreateResponse = PublicApiTokensRevokeFormattedCreateResponses[keyof PublicApiTokensRevokeFormattedCreateResponses];

export type PublicOrganizationsEventsListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        organization_id: string;
    };
    query?: {
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/public/organizations/{organization_id}/events/';
};

export type PublicOrganizationsEventsListResponses = {
    200: PaginatedCalendarEventList;
};

export type PublicOrganizationsEventsListResponse = PublicOrganizationsEventsListResponses[keyof PublicOrganizationsEventsListResponses];

export type PublicOrganizationsEventsCreateData = {
    body: CalendarEventWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        organization_id: string;
    };
    query?: never;
    url: '/public/organizations/{organization_id}/events/';
};

export type PublicOrganizationsEventsCreateResponses = {
    201: CalendarEvent;
};

export type PublicOrganizationsEventsCreateResponse = PublicOrganizationsEventsCreateResponses[keyof PublicOrganizationsEventsCreateResponses];

export type PublicOrganizationsEventsDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        /**
         * A unique integer value identifying this calendar event.
         */
        id: number;
        organization_id: string;
    };
    query?: never;
    url: '/public/organizations/{organization_id}/events/{id}/';
};

export type PublicOrganizationsEventsDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type PublicOrganizationsEventsDestroyResponse = PublicOrganizationsEventsDestroyResponses[keyof PublicOrganizationsEventsDestroyResponses];

export type PublicOrganizationsEventsRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        /**
         * A unique integer value identifying this calendar event.
         */
        id: number;
        organization_id: string;
    };
    query?: never;
    url: '/public/organizations/{organization_id}/events/{id}/';
};

export type PublicOrganizationsEventsRetrieveResponses = {
    200: CalendarEvent;
};

export type PublicOrganizationsEventsRetrieveResponse = PublicOrganizationsEventsRetrieveResponses[keyof PublicOrganizationsEventsRetrieveResponses];

export type PublicOrganizationsEventsPartialUpdateData = {
    body?: PatchedCalendarEventWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        /**
         * A unique integer value identifying this calendar event.
         */
        id: number;
        organization_id: string;
    };
    query?: never;
    url: '/public/organizations/{organization_id}/events/{id}/';
};

export type PublicOrganizationsEventsPartialUpdateResponses = {
    200: CalendarEvent;
};

export type PublicOrganizationsEventsPartialUpdateResponse = PublicOrganizationsEventsPartialUpdateResponses[keyof PublicOrganizationsEventsPartialUpdateResponses];

export type PublicOrganizationsEventsUpdateData = {
    body: CalendarEventWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        /**
         * A unique integer value identifying this calendar event.
         */
        id: number;
        organization_id: string;
    };
    query?: never;
    url: '/public/organizations/{organization_id}/events/{id}/';
};

export type PublicOrganizationsEventsUpdateResponses = {
    200: CalendarEvent;
};

export type PublicOrganizationsEventsUpdateResponse = PublicOrganizationsEventsUpdateResponses[keyof PublicOrganizationsEventsUpdateResponses];

export type ServiceAccountsListData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/service-accounts/';
};

export type ServiceAccountsListResponses = {
    200: PaginatedServiceAccountReadList;
};

export type ServiceAccountsListResponse = ServiceAccountsListResponses[keyof ServiceAccountsListResponses];

export type ServiceAccountsCreateData = {
    body: ServiceAccountWriteWritable;
    path?: never;
    query?: never;
    url: '/service-accounts/';
};

export type ServiceAccountsCreateResponses = {
    201: ServiceAccountRead;
};

export type ServiceAccountsCreateResponse = ServiceAccountsCreateResponses[keyof ServiceAccountsCreateResponses];

export type ServiceAccountsFormattedListData = {
    body?: never;
    path: {
        format: '.json';
    };
    query?: {
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/service-accounts{format}';
};

export type ServiceAccountsFormattedListResponses = {
    200: PaginatedServiceAccountReadList;
};

export type ServiceAccountsFormattedListResponse = ServiceAccountsFormattedListResponses[keyof ServiceAccountsFormattedListResponses];

export type ServiceAccountsFormattedCreateData = {
    body: ServiceAccountWriteWritable;
    path: {
        format: '.json';
    };
    query?: never;
    url: '/service-accounts{format}';
};

export type ServiceAccountsFormattedCreateResponses = {
    201: ServiceAccountRead;
};

export type ServiceAccountsFormattedCreateResponse = ServiceAccountsFormattedCreateResponses[keyof ServiceAccountsFormattedCreateResponses];

export type ServiceAccountsDestroyData = {
    body?: never;
    path: {
        id: string;
    };
    query?: never;
    url: '/service-accounts/{id}/';
};

export type ServiceAccountsDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type ServiceAccountsDestroyResponse = ServiceAccountsDestroyResponses[keyof ServiceAccountsDestroyResponses];

export type ServiceAccountsRetrieveData = {
    body?: never;
    path: {
        id: string;
    };
    query?: never;
    url: '/service-accounts/{id}/';
};

export type ServiceAccountsRetrieveResponses = {
    200: ServiceAccountRead;
};

export type ServiceAccountsRetrieveResponse = ServiceAccountsRetrieveResponses[keyof ServiceAccountsRetrieveResponses];

export type ServiceAccountsPartialUpdateData = {
    body?: PatchedServiceAccountWriteWritable;
    path: {
        id: string;
    };
    query?: never;
    url: '/service-accounts/{id}/';
};

export type ServiceAccountsPartialUpdateResponses = {
    200: ServiceAccountWrite;
};

export type ServiceAccountsPartialUpdateResponse = ServiceAccountsPartialUpdateResponses[keyof ServiceAccountsPartialUpdateResponses];

export type ServiceAccountsUpdateData = {
    body: ServiceAccountWriteWritable;
    path: {
        id: string;
    };
    query?: never;
    url: '/service-accounts/{id}/';
};

export type ServiceAccountsUpdateResponses = {
    200: ServiceAccountRead;
};

export type ServiceAccountsUpdateResponse = ServiceAccountsUpdateResponses[keyof ServiceAccountsUpdateResponses];

export type ServiceAccountsFormattedDestroyData = {
    body?: never;
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/service-accounts/{id}{format}';
};

export type ServiceAccountsFormattedDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type ServiceAccountsFormattedDestroyResponse = ServiceAccountsFormattedDestroyResponses[keyof ServiceAccountsFormattedDestroyResponses];

export type ServiceAccountsFormattedRetrieveData = {
    body?: never;
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/service-accounts/{id}{format}';
};

export type ServiceAccountsFormattedRetrieveResponses = {
    200: ServiceAccountRead;
};

export type ServiceAccountsFormattedRetrieveResponse = ServiceAccountsFormattedRetrieveResponses[keyof ServiceAccountsFormattedRetrieveResponses];

export type ServiceAccountsFormattedPartialUpdateData = {
    body?: PatchedServiceAccountWriteWritable;
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/service-accounts/{id}{format}';
};

export type ServiceAccountsFormattedPartialUpdateResponses = {
    200: ServiceAccountWrite;
};

export type ServiceAccountsFormattedPartialUpdateResponse = ServiceAccountsFormattedPartialUpdateResponses[keyof ServiceAccountsFormattedPartialUpdateResponses];

export type ServiceAccountsFormattedUpdateData = {
    body: ServiceAccountWriteWritable;
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/service-accounts/{id}{format}';
};

export type ServiceAccountsFormattedUpdateResponses = {
    200: ServiceAccountRead;
};

export type ServiceAccountsFormattedUpdateResponse = ServiceAccountsFormattedUpdateResponses[keyof ServiceAccountsFormattedUpdateResponses];

export type WebhookConfigurationsListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/webhook-configurations/';
};

export type WebhookConfigurationsListResponses = {
    200: PaginatedWebhookConfigurationList;
};

export type WebhookConfigurationsListResponse = WebhookConfigurationsListResponses[keyof WebhookConfigurationsListResponses];

export type WebhookConfigurationsCreateData = {
    body: WebhookConfigurationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/webhook-configurations/';
};

export type WebhookConfigurationsCreateResponses = {
    201: WebhookConfiguration;
};

export type WebhookConfigurationsCreateResponse = WebhookConfigurationsCreateResponses[keyof WebhookConfigurationsCreateResponses];

export type WebhookConfigurationsFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: {
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/webhook-configurations{format}';
};

export type WebhookConfigurationsFormattedListResponses = {
    200: PaginatedWebhookConfigurationList;
};

export type WebhookConfigurationsFormattedListResponse = WebhookConfigurationsFormattedListResponses[keyof WebhookConfigurationsFormattedListResponses];

export type WebhookConfigurationsFormattedCreateData = {
    body: WebhookConfigurationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: never;
    url: '/webhook-configurations{format}';
};

export type WebhookConfigurationsFormattedCreateResponses = {
    201: WebhookConfiguration;
};

export type WebhookConfigurationsFormattedCreateResponse = WebhookConfigurationsFormattedCreateResponses[keyof WebhookConfigurationsFormattedCreateResponses];

export type WebhookConfigurationsDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/webhook-configurations/{id}/';
};

export type WebhookConfigurationsDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type WebhookConfigurationsDestroyResponse = WebhookConfigurationsDestroyResponses[keyof WebhookConfigurationsDestroyResponses];

export type WebhookConfigurationsRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/webhook-configurations/{id}/';
};

export type WebhookConfigurationsRetrieveResponses = {
    200: WebhookConfiguration;
};

export type WebhookConfigurationsRetrieveResponse = WebhookConfigurationsRetrieveResponses[keyof WebhookConfigurationsRetrieveResponses];

export type WebhookConfigurationsPartialUpdateData = {
    body?: PatchedWebhookConfigurationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/webhook-configurations/{id}/';
};

export type WebhookConfigurationsPartialUpdateResponses = {
    200: WebhookConfiguration;
};

export type WebhookConfigurationsPartialUpdateResponse = WebhookConfigurationsPartialUpdateResponses[keyof WebhookConfigurationsPartialUpdateResponses];

export type WebhookConfigurationsUpdateData = {
    body: WebhookConfigurationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/webhook-configurations/{id}/';
};

export type WebhookConfigurationsUpdateResponses = {
    200: WebhookConfiguration;
};

export type WebhookConfigurationsUpdateResponse = WebhookConfigurationsUpdateResponses[keyof WebhookConfigurationsUpdateResponses];

export type WebhookConfigurationsFormattedDestroyData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/webhook-configurations/{id}{format}';
};

export type WebhookConfigurationsFormattedDestroyResponses = {
    /**
     * No response body
     */
    204: void;
};

export type WebhookConfigurationsFormattedDestroyResponse = WebhookConfigurationsFormattedDestroyResponses[keyof WebhookConfigurationsFormattedDestroyResponses];

export type WebhookConfigurationsFormattedRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/webhook-configurations/{id}{format}';
};

export type WebhookConfigurationsFormattedRetrieveResponses = {
    200: WebhookConfiguration;
};

export type WebhookConfigurationsFormattedRetrieveResponse = WebhookConfigurationsFormattedRetrieveResponses[keyof WebhookConfigurationsFormattedRetrieveResponses];

export type WebhookConfigurationsFormattedPartialUpdateData = {
    body?: PatchedWebhookConfigurationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/webhook-configurations/{id}{format}';
};

export type WebhookConfigurationsFormattedPartialUpdateResponses = {
    200: WebhookConfiguration;
};

export type WebhookConfigurationsFormattedPartialUpdateResponse = WebhookConfigurationsFormattedPartialUpdateResponses[keyof WebhookConfigurationsFormattedPartialUpdateResponses];

export type WebhookConfigurationsFormattedUpdateData = {
    body: WebhookConfigurationWritable;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/webhook-configurations/{id}{format}';
};

export type WebhookConfigurationsFormattedUpdateResponses = {
    200: WebhookConfiguration;
};

export type WebhookConfigurationsFormattedUpdateResponse = WebhookConfigurationsFormattedUpdateResponses[keyof WebhookConfigurationsFormattedUpdateResponses];

export type WebhookEventsListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/webhook-events/';
};

export type WebhookEventsListResponses = {
    200: PaginatedWebhookEventList;
};

export type WebhookEventsListResponse = WebhookEventsListResponses[keyof WebhookEventsListResponses];

export type WebhookEventsFormattedListData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
    };
    query?: {
        /**
         * Number of results to return per page.
         */
        limit?: number;
        /**
         * The initial index from which to return the results.
         */
        offset?: number;
    };
    url: '/webhook-events{format}';
};

export type WebhookEventsFormattedListResponses = {
    200: PaginatedWebhookEventList;
};

export type WebhookEventsFormattedListResponse = WebhookEventsFormattedListResponses[keyof WebhookEventsFormattedListResponses];

export type WebhookEventsRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/webhook-events/{id}/';
};

export type WebhookEventsRetrieveResponses = {
    200: WebhookEvent;
};

export type WebhookEventsRetrieveResponse = WebhookEventsRetrieveResponses[keyof WebhookEventsRetrieveResponses];

export type WebhookEventsFormattedRetrieveData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/webhook-events/{id}{format}';
};

export type WebhookEventsFormattedRetrieveResponses = {
    200: WebhookEvent;
};

export type WebhookEventsFormattedRetrieveResponse = WebhookEventsFormattedRetrieveResponses[keyof WebhookEventsFormattedRetrieveResponses];

export type WebhookEventsRetryCreateData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        id: string;
    };
    query?: never;
    url: '/webhook-events/{id}/retry/';
};

export type WebhookEventsRetryCreateErrors = {
    /**
     * Bad request - event cannot be retried
     */
    400: unknown;
    /**
     * Event not found
     */
    404: unknown;
};

export type WebhookEventsRetryCreateResponses = {
    200: WebhookEvent;
};

export type WebhookEventsRetryCreateResponse = WebhookEventsRetryCreateResponses[keyof WebhookEventsRetryCreateResponses];

export type WebhookEventsRetryFormattedCreateData = {
    body?: never;
    headers?: {
        /**
         * Selects the active organization for this request. Optional for callers that belong to exactly one active organization — the single membership is resolved implicitly. **Required** when the caller has two or more active memberships; omitting it in that case returns **400**. If the header names an organization the caller is not an active member of, the server returns **403**.
         */
        'X-Organization-Id'?: string;
    };
    path: {
        format: '.json';
        id: string;
    };
    query?: never;
    url: '/webhook-events/{id}/retry{format}';
};

export type WebhookEventsRetryFormattedCreateErrors = {
    /**
     * Bad request - event cannot be retried
     */
    400: unknown;
    /**
     * Event not found
     */
    404: unknown;
};

export type WebhookEventsRetryFormattedCreateResponses = {
    200: WebhookEvent;
};

export type WebhookEventsRetryFormattedCreateResponse = WebhookEventsRetryFormattedCreateResponses[keyof WebhookEventsRetryFormattedCreateResponses];
