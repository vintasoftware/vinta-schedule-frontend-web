// This file is auto-generated by @hey-api/openapi-ts

import { type Client, type Options as Options2, type RequestResult, type TDataShape, urlSearchParamsBodySerializer } from './client';
import { client } from './client.gen';
import type { DeleteAuthByClientV1AccountAuthenticatorsTotpData, DeleteAuthByClientV1AccountAuthenticatorsTotpErrors, DeleteAuthByClientV1AccountAuthenticatorsTotpResponses, DeleteAuthByClientV1AccountEmailData, DeleteAuthByClientV1AccountEmailErrors, DeleteAuthByClientV1AccountEmailResponses, DeleteAuthByClientV1AccountProvidersData, DeleteAuthByClientV1AccountProvidersErrors, DeleteAuthByClientV1AccountProvidersResponses, DeleteAuthByClientV1AuthSessionData, DeleteAuthByClientV1AuthSessionErrors, GetAuthByClientV1AccountAuthenticatorsData, GetAuthByClientV1AccountAuthenticatorsErrors, GetAuthByClientV1AccountAuthenticatorsRecoveryCodesData, GetAuthByClientV1AccountAuthenticatorsRecoveryCodesErrors, GetAuthByClientV1AccountAuthenticatorsRecoveryCodesResponses, GetAuthByClientV1AccountAuthenticatorsResponses, GetAuthByClientV1AccountAuthenticatorsTotpData, GetAuthByClientV1AccountAuthenticatorsTotpErrors, GetAuthByClientV1AccountAuthenticatorsTotpResponses, GetAuthByClientV1AccountEmailData, GetAuthByClientV1AccountEmailErrors, GetAuthByClientV1AccountEmailResponses, GetAuthByClientV1AccountPhoneData, GetAuthByClientV1AccountPhoneErrors, GetAuthByClientV1AccountPhoneResponses, GetAuthByClientV1AccountProvidersData, GetAuthByClientV1AccountProvidersResponses, GetAuthByClientV1AuthEmailVerifyData, GetAuthByClientV1AuthEmailVerifyErrors, GetAuthByClientV1AuthEmailVerifyResponses, GetAuthByClientV1AuthPasswordResetData, GetAuthByClientV1AuthPasswordResetErrors, GetAuthByClientV1AuthPasswordResetResponses, GetAuthByClientV1AuthProviderSignupData, GetAuthByClientV1AuthProviderSignupErrors, GetAuthByClientV1AuthProviderSignupResponses, GetAuthByClientV1AuthSessionData, GetAuthByClientV1AuthSessionErrors, GetAuthByClientV1AuthSessionResponses, GetAuthByClientV1ConfigData, GetAuthByClientV1ConfigResponses, PatchAuthByClientV1AccountEmailData, PatchAuthByClientV1AccountEmailErrors, PatchAuthByClientV1AccountEmailResponses, PostAuthAppV1TokensRefreshData, PostAuthAppV1TokensRefreshErrors, PostAuthAppV1TokensRefreshResponses, PostAuthBrowserV1AuthProviderRedirectData, PostAuthByClientV1AccountAuthenticatorsRecoveryCodesData, PostAuthByClientV1AccountAuthenticatorsRecoveryCodesErrors, PostAuthByClientV1AccountAuthenticatorsTotpData, PostAuthByClientV1AccountAuthenticatorsTotpErrors, PostAuthByClientV1AccountAuthenticatorsTotpResponses, PostAuthByClientV1AccountEmailData, PostAuthByClientV1AccountEmailErrors, PostAuthByClientV1AccountEmailResponses, PostAuthByClientV1AccountPasswordChangeData, PostAuthByClientV1AccountPasswordChangeErrors, PostAuthByClientV1AccountPhoneData, PostAuthByClientV1AccountPhoneErrors, PostAuthByClientV1AccountPhoneResponses, PostAuthByClientV1Auth2FaAuthenticateData, PostAuthByClientV1Auth2FaAuthenticateErrors, PostAuthByClientV1Auth2FaAuthenticateResponses, PostAuthByClientV1Auth2FaReauthenticateData, PostAuthByClientV1Auth2FaReauthenticateErrors, PostAuthByClientV1Auth2FaReauthenticateResponses, PostAuthByClientV1AuthCodeConfirmData, PostAuthByClientV1AuthCodeConfirmErrors, PostAuthByClientV1AuthCodeConfirmResponses, PostAuthByClientV1AuthEmailVerifyData, PostAuthByClientV1AuthEmailVerifyErrors, PostAuthByClientV1AuthEmailVerifyResendData, PostAuthByClientV1AuthEmailVerifyResendErrors, PostAuthByClientV1AuthEmailVerifyResendResponses, PostAuthByClientV1AuthEmailVerifyResponses, PostAuthByClientV1AuthLoginData, PostAuthByClientV1AuthLoginErrors, PostAuthByClientV1AuthLoginResponses, PostAuthByClientV1AuthPasswordRequestData, PostAuthByClientV1AuthPasswordRequestErrors, PostAuthByClientV1AuthPasswordRequestResponses, PostAuthByClientV1AuthPasswordResetData, PostAuthByClientV1AuthPasswordResetErrors, PostAuthByClientV1AuthPasswordResetResponses, PostAuthByClientV1AuthPhoneVerifyData, PostAuthByClientV1AuthPhoneVerifyErrors, PostAuthByClientV1AuthPhoneVerifyResendData, PostAuthByClientV1AuthPhoneVerifyResendErrors, PostAuthByClientV1AuthPhoneVerifyResendResponses, PostAuthByClientV1AuthPhoneVerifyResponses, PostAuthByClientV1AuthProviderSignupData, PostAuthByClientV1AuthProviderSignupErrors, PostAuthByClientV1AuthProviderSignupResponses, PostAuthByClientV1AuthProviderTokenData, PostAuthByClientV1AuthProviderTokenErrors, PostAuthByClientV1AuthProviderTokenResponses, PostAuthByClientV1AuthReauthenticateData, PostAuthByClientV1AuthReauthenticateErrors, PostAuthByClientV1AuthReauthenticateResponses, PostAuthByClientV1AuthSignupData, PostAuthByClientV1AuthSignupErrors, PostAuthByClientV1AuthSignupResponses, PutAuthByClientV1AccountEmailData, PutAuthByClientV1AccountEmailErrors, PutAuthByClientV1AccountEmailResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
  /**
   * You can provide a client instance returned by `createClient()` instead of
   * individual options. This might be also useful if you want to implement a
   * custom client.
   */
  client?: Client;
  /**
   * You can pass arbitrary values through the `meta` object. This can be
   * used to access values that aren't defined as part of the SDK function.
   */
  meta?: Record<string, unknown>;
};

/**
 * Refresh the access token
 *
 *
 * Used to retrieve a new access token. Depending on `settings.HEADLESS_JWT_ROTATE_REFRESH_TOKEN`,
 * a new refresh token is returned as well.
 *
 */
export const postAuthAppV1TokensRefresh = <ThrowOnError extends boolean = false>(options?: Options<PostAuthAppV1TokensRefreshData, ThrowOnError>): RequestResult<PostAuthAppV1TokensRefreshResponses, PostAuthAppV1TokensRefreshErrors, ThrowOnError> => (options?.client ?? client).post<PostAuthAppV1TokensRefreshResponses, PostAuthAppV1TokensRefreshErrors, ThrowOnError>({
  url: '/auth/app/v1/tokens/refresh',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options?.headers
  }
});

/**
 * Provider redirect
 *
 * Initiates the third-party provider authentication redirect flow. As calling
 * this endpoint results in a user facing redirect (302), this call is only
 * available in a browser, and must be called in a synchronous (non-XHR)
 * manner.
 *
 */
export const postAuthBrowserV1AuthProviderRedirect = <ThrowOnError extends boolean = false>(options: Options<PostAuthBrowserV1AuthProviderRedirectData, ThrowOnError>): RequestResult<unknown, unknown, ThrowOnError> => (options.client ?? client).post<unknown, unknown, ThrowOnError>({
  ...urlSearchParamsBodySerializer,
  url: '/auth/browser/v1/auth/provider/redirect',
  ...options,
  headers: {
    'Content-Type': 'application/x-www-form-urlencoded',
    ...options.headers
  }
});

/**
 * List authenticators
 */
export const getAuthByClientV1AccountAuthenticators = <ThrowOnError extends boolean = false>(options: Options<GetAuthByClientV1AccountAuthenticatorsData, ThrowOnError>): RequestResult<GetAuthByClientV1AccountAuthenticatorsResponses, GetAuthByClientV1AccountAuthenticatorsErrors, ThrowOnError> => (options.client ?? client).get<GetAuthByClientV1AccountAuthenticatorsResponses, GetAuthByClientV1AccountAuthenticatorsErrors, ThrowOnError>({ url: '/auth/{client}/v1/account/authenticators', ...options });

/**
 * List recovery codes
 *
 * List recovery codes.
 *
 */
export const getAuthByClientV1AccountAuthenticatorsRecoveryCodes = <ThrowOnError extends boolean = false>(options: Options<GetAuthByClientV1AccountAuthenticatorsRecoveryCodesData, ThrowOnError>): RequestResult<GetAuthByClientV1AccountAuthenticatorsRecoveryCodesResponses, GetAuthByClientV1AccountAuthenticatorsRecoveryCodesErrors, ThrowOnError> => (options.client ?? client).get<GetAuthByClientV1AccountAuthenticatorsRecoveryCodesResponses, GetAuthByClientV1AccountAuthenticatorsRecoveryCodesErrors, ThrowOnError>({ url: '/auth/{client}/v1/account/authenticators/recovery-codes', ...options });

/**
 * Regenerate recovery codes
 */
export const postAuthByClientV1AccountAuthenticatorsRecoveryCodes = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AccountAuthenticatorsRecoveryCodesData, ThrowOnError>): RequestResult<unknown, PostAuthByClientV1AccountAuthenticatorsRecoveryCodesErrors, ThrowOnError> => (options.client ?? client).post<unknown, PostAuthByClientV1AccountAuthenticatorsRecoveryCodesErrors, ThrowOnError>({ url: '/auth/{client}/v1/account/authenticators/recovery-codes', ...options });

/**
 * Deactivate TOTP
 *
 * Deactivates TOTP authentication. If the user authentication is not
 * sufficiently recent, a reauthentication flow (`401`) will is presented.
 *
 */
export const deleteAuthByClientV1AccountAuthenticatorsTotp = <ThrowOnError extends boolean = false>(options: Options<DeleteAuthByClientV1AccountAuthenticatorsTotpData, ThrowOnError>): RequestResult<DeleteAuthByClientV1AccountAuthenticatorsTotpResponses, DeleteAuthByClientV1AccountAuthenticatorsTotpErrors, ThrowOnError> => (options.client ?? client).delete<DeleteAuthByClientV1AccountAuthenticatorsTotpResponses, DeleteAuthByClientV1AccountAuthenticatorsTotpErrors, ThrowOnError>({ url: '/auth/{client}/v1/account/authenticators/totp', ...options });

/**
 * TOTP authenticator status
 *
 * Retrieve the information about the current TOTP authenticator, if any.
 *
 */
export const getAuthByClientV1AccountAuthenticatorsTotp = <ThrowOnError extends boolean = false>(options: Options<GetAuthByClientV1AccountAuthenticatorsTotpData, ThrowOnError>): RequestResult<GetAuthByClientV1AccountAuthenticatorsTotpResponses, GetAuthByClientV1AccountAuthenticatorsTotpErrors, ThrowOnError> => (options.client ?? client).get<GetAuthByClientV1AccountAuthenticatorsTotpResponses, GetAuthByClientV1AccountAuthenticatorsTotpErrors, ThrowOnError>({ url: '/auth/{client}/v1/account/authenticators/totp', ...options });

/**
 * Activate TOTP
 *
 * The code should be provided from the consuming TOTP authenticator
 * application which was generated using the TOTP authenticator secret
 * retrieved from the TOTP authenticator status endpoint.
 *
 */
export const postAuthByClientV1AccountAuthenticatorsTotp = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AccountAuthenticatorsTotpData, ThrowOnError>): RequestResult<PostAuthByClientV1AccountAuthenticatorsTotpResponses, PostAuthByClientV1AccountAuthenticatorsTotpErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AccountAuthenticatorsTotpResponses, PostAuthByClientV1AccountAuthenticatorsTotpErrors, ThrowOnError>({
  url: '/auth/{client}/v1/account/authenticators/totp',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Remove an email address
 *
 * Used to remove an email address.
 *
 */
export const deleteAuthByClientV1AccountEmail = <ThrowOnError extends boolean = false>(options: Options<DeleteAuthByClientV1AccountEmailData, ThrowOnError>): RequestResult<DeleteAuthByClientV1AccountEmailResponses, DeleteAuthByClientV1AccountEmailErrors, ThrowOnError> => (options.client ?? client).delete<DeleteAuthByClientV1AccountEmailResponses, DeleteAuthByClientV1AccountEmailErrors, ThrowOnError>({
  url: '/auth/{client}/v1/account/email',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * List email addresses
 *
 * Retrieves the list of email addresses of the account.
 *
 */
export const getAuthByClientV1AccountEmail = <ThrowOnError extends boolean = false>(options: Options<GetAuthByClientV1AccountEmailData, ThrowOnError>): RequestResult<GetAuthByClientV1AccountEmailResponses, GetAuthByClientV1AccountEmailErrors, ThrowOnError> => (options.client ?? client).get<GetAuthByClientV1AccountEmailResponses, GetAuthByClientV1AccountEmailErrors, ThrowOnError>({ url: '/auth/{client}/v1/account/email', ...options });

/**
 * Change primary email address
 *
 * Used to change primary email address to a different one. Note that only verified email addresses
 * can be marked as primary.
 *
 */
export const patchAuthByClientV1AccountEmail = <ThrowOnError extends boolean = false>(options: Options<PatchAuthByClientV1AccountEmailData, ThrowOnError>): RequestResult<PatchAuthByClientV1AccountEmailResponses, PatchAuthByClientV1AccountEmailErrors, ThrowOnError> => (options.client ?? client).patch<PatchAuthByClientV1AccountEmailResponses, PatchAuthByClientV1AccountEmailErrors, ThrowOnError>({
  url: '/auth/{client}/v1/account/email',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Add/Change email address
 *
 *
 * The following functionality is available:
 *
 * - Adding a new email address for an already signed in user (`ACCOUNT_CHANGE_EMAIL = False`).
 * - Change to a new email address for an already signed in user   (`ACCOUNT_CHANGE_EMAIL = True`).
 * - Change to a new email address during the email verification process at signup (`ACCOUNT_EMAIL_VERIFICATION_SUPPORTS_CHANGE = True`).
 *
 * In all cases, an email verification mail will be sent containing a link or code that needs to be verified.
 *
 */
export const postAuthByClientV1AccountEmail = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AccountEmailData, ThrowOnError>): RequestResult<PostAuthByClientV1AccountEmailResponses, PostAuthByClientV1AccountEmailErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AccountEmailResponses, PostAuthByClientV1AccountEmailErrors, ThrowOnError>({
  url: '/auth/{client}/v1/account/email',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Request email verification
 *
 * Requests for (another) email verification email to be sent. Note that
 * sending emails is rate limited, so when you send too many requests the
 * email will not be sent.
 *
 */
export const putAuthByClientV1AccountEmail = <ThrowOnError extends boolean = false>(options: Options<PutAuthByClientV1AccountEmailData, ThrowOnError>): RequestResult<PutAuthByClientV1AccountEmailResponses, PutAuthByClientV1AccountEmailErrors, ThrowOnError> => (options.client ?? client).put<PutAuthByClientV1AccountEmailResponses, PutAuthByClientV1AccountEmailErrors, ThrowOnError>({
  url: '/auth/{client}/v1/account/email',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Change password
 *
 * In order to change the password of an account, the current and new
 * password must be provider.  However, accounts that were created by
 * signing up using a third-party provider do not have a password set. In
 * that case, the current password is not required.
 *
 */
export const postAuthByClientV1AccountPasswordChange = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AccountPasswordChangeData, ThrowOnError>): RequestResult<unknown, PostAuthByClientV1AccountPasswordChangeErrors, ThrowOnError> => (options.client ?? client).post<unknown, PostAuthByClientV1AccountPasswordChangeErrors, ThrowOnError>({
  url: '/auth/{client}/v1/account/password/change',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Get the phone number
 *
 * Retrieves the phone number of the account, if any. Note that while the
 * endpoint returns a list of phone numbers, at most one entry is returned.
 *
 */
export const getAuthByClientV1AccountPhone = <ThrowOnError extends boolean = false>(options: Options<GetAuthByClientV1AccountPhoneData, ThrowOnError>): RequestResult<GetAuthByClientV1AccountPhoneResponses, GetAuthByClientV1AccountPhoneErrors, ThrowOnError> => (options.client ?? client).get<GetAuthByClientV1AccountPhoneResponses, GetAuthByClientV1AccountPhoneErrors, ThrowOnError>({ url: '/auth/{client}/v1/account/phone', ...options });

/**
 * Change the phone number
 *
 *
 * The following functionality is available:
 *
 * - Initiate the phone number change process for signed in users.
 * - Change to a new phone number during the phone number verification
 * process at signup for unauthenticated users. Note that this requires:
 * `ACCOUNT_PHONE_VERIFICATION_SUPPORTS_CHANGE = True`.
 *
 * In both cases, after posting a new phone number, proceed with the phone
 * verification endpoint to confirm the change of the phone number by
 * posting the verification code.
 *
 */
export const postAuthByClientV1AccountPhone = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AccountPhoneData, ThrowOnError>): RequestResult<PostAuthByClientV1AccountPhoneResponses, PostAuthByClientV1AccountPhoneErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AccountPhoneResponses, PostAuthByClientV1AccountPhoneErrors, ThrowOnError>({
  url: '/auth/{client}/v1/account/phone',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Disconnect a third-party provider account
 *
 *
 * Disconnect a third-party provider account, returning the remaining
 * accounts that are still connected. The disconnect is not allowed if it
 * would leave the account unusable. For example, if no password was
 * set up yet.
 *
 */
export const deleteAuthByClientV1AccountProviders = <ThrowOnError extends boolean = false>(options: Options<DeleteAuthByClientV1AccountProvidersData, ThrowOnError>): RequestResult<DeleteAuthByClientV1AccountProvidersResponses, DeleteAuthByClientV1AccountProvidersErrors, ThrowOnError> => (options.client ?? client).delete<DeleteAuthByClientV1AccountProvidersResponses, DeleteAuthByClientV1AccountProvidersErrors, ThrowOnError>({
  url: '/auth/{client}/v1/account/providers',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * List the connected third-party provider accounts
 */
export const getAuthByClientV1AccountProviders = <ThrowOnError extends boolean = false>(options: Options<GetAuthByClientV1AccountProvidersData, ThrowOnError>): RequestResult<GetAuthByClientV1AccountProvidersResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetAuthByClientV1AccountProvidersResponses, unknown, ThrowOnError>({ url: '/auth/{client}/v1/account/providers', ...options });

/**
 * Two-factor authentication
 *
 * If, during authentication,  a response with status 401 is encountered where one of the pending
 * flows has ID `mfa_authenticate`, that indicates that the Two-Factor Authentication stage needs to
 * be completed.
 *
 */
export const postAuthByClientV1Auth2FaAuthenticate = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1Auth2FaAuthenticateData, ThrowOnError>): RequestResult<PostAuthByClientV1Auth2FaAuthenticateResponses, PostAuthByClientV1Auth2FaAuthenticateErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1Auth2FaAuthenticateResponses, PostAuthByClientV1Auth2FaAuthenticateErrors, ThrowOnError>({
  url: '/auth/{client}/v1/auth/2fa/authenticate',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Reauthenticate using 2FA
 *
 * In order to safeguard the account, some actions require the user to be
 * recently authenticated.  If you try to perform such an action without
 * having been recently authenticated, a `401` status is returned, listing
 * flows that can be performed to reauthenticate. One such flow is the flow
 * with ID `mfa_reauthenticate`, which allows for the user to input an
 * authenticator code (e.g. TOTP or recovery code). This is the endpoint
 * related towards that flow.
 *
 */
export const postAuthByClientV1Auth2FaReauthenticate = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1Auth2FaReauthenticateData, ThrowOnError>): RequestResult<PostAuthByClientV1Auth2FaReauthenticateResponses, PostAuthByClientV1Auth2FaReauthenticateErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1Auth2FaReauthenticateResponses, PostAuthByClientV1Auth2FaReauthenticateErrors, ThrowOnError>({
  url: '/auth/{client}/v1/auth/2fa/reauthenticate',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Confirm login code
 *
 * Use this endpoint to pass along the received "special" login code.
 *
 */
export const postAuthByClientV1AuthCodeConfirm = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AuthCodeConfirmData, ThrowOnError>): RequestResult<PostAuthByClientV1AuthCodeConfirmResponses, PostAuthByClientV1AuthCodeConfirmErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AuthCodeConfirmResponses, PostAuthByClientV1AuthCodeConfirmErrors, ThrowOnError>({
  url: '/auth/{client}/v1/auth/code/confirm',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Get email verification information
 *
 * Obtain email verification information, given the token that was sent to
 * the user by email.
 *
 */
export const getAuthByClientV1AuthEmailVerify = <ThrowOnError extends boolean = false>(options: Options<GetAuthByClientV1AuthEmailVerifyData, ThrowOnError>): RequestResult<GetAuthByClientV1AuthEmailVerifyResponses, GetAuthByClientV1AuthEmailVerifyErrors, ThrowOnError> => (options.client ?? client).get<GetAuthByClientV1AuthEmailVerifyResponses, GetAuthByClientV1AuthEmailVerifyErrors, ThrowOnError>({ url: '/auth/{client}/v1/auth/email/verify', ...options });

/**
 * Verify an email
 *
 * Complete the email verification process. Depending on the configuration,
 * email addresses are either verified by opening a link that is sent to
 * their email address, or, by inputting a code that is sent. On the API,
 * both cases are handled identically. Meaning, the required key is either
 * the one from the link, or, the code itself.
 *
 * Note that a status code of 401 does not imply failure. It indicates that
 * the email verification was successful, yet, the user is still not signed
 * in. For example, in case `ACCOUNT_LOGIN_ON_EMAIL_CONFIRMATION` is set to
 * `False`, a 401 is returned when verifying as part of login/signup.
 *
 */
export const postAuthByClientV1AuthEmailVerify = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AuthEmailVerifyData, ThrowOnError>): RequestResult<PostAuthByClientV1AuthEmailVerifyResponses, PostAuthByClientV1AuthEmailVerifyErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AuthEmailVerifyResponses, PostAuthByClientV1AuthEmailVerifyErrors, ThrowOnError>({
  url: '/auth/{client}/v1/auth/email/verify',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Resend email verification code
 *
 * Requests a new email verification code.
 * Requires `ACCOUNT_EMAIL_VERIFICATION_SUPPORTS_RESEND = True`.
 *
 */
export const postAuthByClientV1AuthEmailVerifyResend = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AuthEmailVerifyResendData, ThrowOnError>): RequestResult<PostAuthByClientV1AuthEmailVerifyResendResponses, PostAuthByClientV1AuthEmailVerifyResendErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AuthEmailVerifyResendResponses, PostAuthByClientV1AuthEmailVerifyResendErrors, ThrowOnError>({ url: '/auth/{client}/v1/auth/email/verify/resend', ...options });

/**
 * Login
 *
 * Login using a username-password or email-password combination.
 *
 */
export const postAuthByClientV1AuthLogin = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AuthLoginData, ThrowOnError>): RequestResult<PostAuthByClientV1AuthLoginResponses, PostAuthByClientV1AuthLoginErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AuthLoginResponses, PostAuthByClientV1AuthLoginErrors, ThrowOnError>({
  url: '/auth/{client}/v1/auth/login',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Request password
 *
 * Initiates the password reset procedure. Depending on whether or not
 * `ACCOUNT_PASSWORD_RESET_BY_CODE_ENABLED` is `True`, the procedure is
 * either stateless or stateful.
 *
 * In case codes are used, it is stateful, and a new
 * `password_reset_by_code` flow is started. In this case, on a successful
 * password reset request, you will receive a 401 indicating the pending
 * status of this flow.
 *
 * In case password reset is configured to use (stateless) links, you will
 * receive a 200 on a successful password reset request.
 *
 */
export const postAuthByClientV1AuthPasswordRequest = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AuthPasswordRequestData, ThrowOnError>): RequestResult<PostAuthByClientV1AuthPasswordRequestResponses, PostAuthByClientV1AuthPasswordRequestErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AuthPasswordRequestResponses, PostAuthByClientV1AuthPasswordRequestErrors, ThrowOnError>({
  url: '/auth/{client}/v1/auth/password/request',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Get password reset information
 *
 * Used to obtain information on and validate a password reset key.  The
 * key passed is either the key encoded in the password reset URL that the
 * user has received per email, or, the password reset code in case of
 * `ACCOUNT_PASSWORD_RESET_BY_CODE_ENABLED`. Note that in case of a code,
 * the number of requests you can make is limited (by
 * `ACCOUNT_PASSWORD_RESET_BY_CODE_MAX_ATTEMPTS`).
 *
 */
export const getAuthByClientV1AuthPasswordReset = <ThrowOnError extends boolean = false>(options: Options<GetAuthByClientV1AuthPasswordResetData, ThrowOnError>): RequestResult<GetAuthByClientV1AuthPasswordResetResponses, GetAuthByClientV1AuthPasswordResetErrors, ThrowOnError> => (options.client ?? client).get<GetAuthByClientV1AuthPasswordResetResponses, GetAuthByClientV1AuthPasswordResetErrors, ThrowOnError>({ url: '/auth/{client}/v1/auth/password/reset', ...options });

/**
 * Reset password
 *
 * Perform the password reset, by handing over the password reset key and
 * the new password. After successfully completing the password reset, the
 * user is either logged in (in case `ACCOUNT_LOGIN_ON_PASSWORD_RESET` is
 * `True`), or, the user will need to proceed to the login page.  In case
 * of the former, a `200` status code is returned, in case of the latter a
 * 401.
 *
 */
export const postAuthByClientV1AuthPasswordReset = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AuthPasswordResetData, ThrowOnError>): RequestResult<PostAuthByClientV1AuthPasswordResetResponses, PostAuthByClientV1AuthPasswordResetErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AuthPasswordResetResponses, PostAuthByClientV1AuthPasswordResetErrors, ThrowOnError>({
  url: '/auth/{client}/v1/auth/password/reset',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Verify a phone number
 *
 * Complete the phone number verification process. Note that a status code
 * of 401 does not imply failure. It merely indicates that the phone number
 * verification was successful, yet, the user is still not signed in.
 *
 */
export const postAuthByClientV1AuthPhoneVerify = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AuthPhoneVerifyData, ThrowOnError>): RequestResult<PostAuthByClientV1AuthPhoneVerifyResponses, PostAuthByClientV1AuthPhoneVerifyErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AuthPhoneVerifyResponses, PostAuthByClientV1AuthPhoneVerifyErrors, ThrowOnError>({
  url: '/auth/{client}/v1/auth/phone/verify',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Resend phone number verification code
 *
 * Requests a new phone number verification code.
 * Requires `ACCOUNT_PHONE_VERIFICATION_SUPPORTS_RESEND = True`.
 *
 */
export const postAuthByClientV1AuthPhoneVerifyResend = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AuthPhoneVerifyResendData, ThrowOnError>): RequestResult<PostAuthByClientV1AuthPhoneVerifyResendResponses, PostAuthByClientV1AuthPhoneVerifyResendErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AuthPhoneVerifyResendResponses, PostAuthByClientV1AuthPhoneVerifyResendErrors, ThrowOnError>({ url: '/auth/{client}/v1/auth/phone/verify/resend', ...options });

/**
 * Provider signup information
 *
 * If, while signing up using a third-party provider account, there is
 * insufficient information received from the provider to automatically
 * complete the signup process, an additional step is needed to complete
 * the missing data before the user is fully signed up and authenticated.
 * The information available so far, such as the pending provider account,
 * can be retrieved via this endpoint.
 *
 */
export const getAuthByClientV1AuthProviderSignup = <ThrowOnError extends boolean = false>(options: Options<GetAuthByClientV1AuthProviderSignupData, ThrowOnError>): RequestResult<GetAuthByClientV1AuthProviderSignupResponses, GetAuthByClientV1AuthProviderSignupErrors, ThrowOnError> => (options.client ?? client).get<GetAuthByClientV1AuthProviderSignupResponses, GetAuthByClientV1AuthProviderSignupErrors, ThrowOnError>({ url: '/auth/{client}/v1/auth/provider/signup', ...options });

/**
 * Provider signup
 *
 * If, while signing up using a third-party provider account, there is
 * insufficient information received from the provider to automatically
 * complete the signup process, an additional step is needed to complete
 * the missing data before the user is fully signed up and authenticated.
 *
 */
export const postAuthByClientV1AuthProviderSignup = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AuthProviderSignupData, ThrowOnError>): RequestResult<PostAuthByClientV1AuthProviderSignupResponses, PostAuthByClientV1AuthProviderSignupErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AuthProviderSignupResponses, PostAuthByClientV1AuthProviderSignupErrors, ThrowOnError>({
  url: '/auth/{client}/v1/auth/provider/signup',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Provider token
 *
 * Authenticates with a third-party provider using provider tokens received
 * by other means. For example, in case of a mobile app, the authentication
 * flow runs completely on the device itself, without any interaction with
 * the API. Then, when the (device) authentication completes and the mobile
 * app receives an access and/or ID token, it can hand over these tokens
 * via this endpoint to authenticate on the server.
 *
 */
export const postAuthByClientV1AuthProviderToken = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AuthProviderTokenData, ThrowOnError>): RequestResult<PostAuthByClientV1AuthProviderTokenResponses, PostAuthByClientV1AuthProviderTokenErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AuthProviderTokenResponses, PostAuthByClientV1AuthProviderTokenErrors, ThrowOnError>({
  url: '/auth/{client}/v1/auth/provider/token',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Reauthenticate
 *
 * In order to safeguard the account, some actions require the user to be
 * recently authenticated.  If you try to perform such an action without
 * having been recently authenticated, a `401` status is returned, listing
 * flows that can be performed to reauthenticate. One such flow is the flow
 * with ID `reauthenticate`, which allows for the user to input the
 * password. This is the endpoint related towards that flow.
 *
 */
export const postAuthByClientV1AuthReauthenticate = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AuthReauthenticateData, ThrowOnError>): RequestResult<PostAuthByClientV1AuthReauthenticateResponses, PostAuthByClientV1AuthReauthenticateErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AuthReauthenticateResponses, PostAuthByClientV1AuthReauthenticateErrors, ThrowOnError>({
  url: '/auth/{client}/v1/auth/reauthenticate',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Logout
 *
 * Logs out the user from the current session.
 *
 */
export const deleteAuthByClientV1AuthSession = <ThrowOnError extends boolean = false>(options: Options<DeleteAuthByClientV1AuthSessionData, ThrowOnError>): RequestResult<unknown, DeleteAuthByClientV1AuthSessionErrors, ThrowOnError> => (options.client ?? client).delete<unknown, DeleteAuthByClientV1AuthSessionErrors, ThrowOnError>({ url: '/auth/{client}/v1/auth/session', ...options });

/**
 * Get authentication status
 *
 *
 * Retrieve information about the authentication status for the current
 * session.
 *
 */
export const getAuthByClientV1AuthSession = <ThrowOnError extends boolean = false>(options: Options<GetAuthByClientV1AuthSessionData, ThrowOnError>): RequestResult<GetAuthByClientV1AuthSessionResponses, GetAuthByClientV1AuthSessionErrors, ThrowOnError> => (options.client ?? client).get<GetAuthByClientV1AuthSessionResponses, GetAuthByClientV1AuthSessionErrors, ThrowOnError>({ url: '/auth/{client}/v1/auth/session', ...options });

/**
 * Signup
 *
 * Whether or not `username`, `email`, `phone` or combination of those are
 * required depends on the configuration of django-allauth. Additionally,
 * if a custom signup form is used there may be other custom properties
 * required.
 *
 */
export const postAuthByClientV1AuthSignup = <ThrowOnError extends boolean = false>(options: Options<PostAuthByClientV1AuthSignupData, ThrowOnError>): RequestResult<PostAuthByClientV1AuthSignupResponses, PostAuthByClientV1AuthSignupErrors, ThrowOnError> => (options.client ?? client).post<PostAuthByClientV1AuthSignupResponses, PostAuthByClientV1AuthSignupErrors, ThrowOnError>({
  url: '/auth/{client}/v1/auth/signup',
  ...options,
  headers: {
    'Content-Type': 'application/json',
    ...options.headers
  }
});

/**
 * Get configuration
 *
 * There are many configuration options that alter the functionality
 * and behavior of django-allauth, some of which can also impact the
 * frontend. Therefore, relevant configuration options are exposed via
 * this endpoint. The data returned is not user/authentication
 * dependent. Hence, it suffices to only fetch this data once at boot
 * time of your application.
 *
 */
export const getAuthByClientV1Config = <ThrowOnError extends boolean = false>(options: Options<GetAuthByClientV1ConfigData, ThrowOnError>): RequestResult<GetAuthByClientV1ConfigResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetAuthByClientV1ConfigResponses, unknown, ThrowOnError>({ url: '/auth/{client}/v1/config', ...options });
